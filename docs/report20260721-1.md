# 任務報告 — 部署更新 + GitHub push(report20260721-1)

> 日期:2026-07-21
> 任務:確認未部署 commit → 更新線上站 → push GitHub

## 背景

線上站 `m-code.ericchh.work` 的 docroot(`/Users/hezhaoxing/m-code-site`)停留在 7/1 建置的版本(約對應 `a954126`,7/2),7/3 之後約 26 個 commit 未上線;另本地 `main` 領先 `origin/main` 3 個 commit 未 push。

## 執行內容

1. **測試**:`npx vitest run` — 12 檔 159 測試全數通過。
2. **建置**:`npm run build`(tsc → `dist/`)。
3. **同步 docroot**:依 `deploy/README-launchagent.md` 標準指令
   `rsync -a --delete --exclude node_modules --exclude .git ./ ~/m-code-site/`
4. **驗證**:
   - `diff -rq` 確認 docroot 的 `dist/`、`demo/`、`src/` 與 repo 完全一致
   - `curl localhost:8765` 回 200,且 `dist/engines/gate.js` 已含 7/20 白平衡新邏輯(伺服器送 `Cache-Control: no-store`,邊緣不會殘留舊檔)
5. **Push**(經 Eric 明確同意):`d5aee2b..ec45ff6  main -> main`,推上 3 個 commit:
   - `ec45ff6` fix(gate): 白平衡改為建議燈,不再鎖死快門
   - `daa5db6` docs: 任務報告 report20260716-1
   - `591da12` feat(demo): 桌面版加登入頁 + 帳密/tier 統一

## 本次上線的主要功能(7/3 → 7/20 累積)

- 手機相機真實掃描(ZXing)+ 影像處理層 + 六燈號分級
- PM 三回審查 P0/P1 修正、操作手冊 manual.html
- 楞痕(washboard)誤判修正、掃描 UX 簡化(類型自動偵測)
- 手機/桌面登入頁 + 帳密與 tier 分級統一
- 白平衡改建議燈不鎖快門

## 結果

- 線上站與 repo `main`(`ec45ff6`)完全一致
- GitHub `origin/main` 與本地同步,無未推 commit
