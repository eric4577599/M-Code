# 任務報告 — 專案 CLAUDE.md + 規格回寫四項(report20260729-1)

> 日期:2026-07-29
> 分支:main(`695511a` → `3a52c2b`,已 push)
> 範圍:專案規則檔、主規劃書、測試;**未動 `src/` 與 `demo/`,線上站不需重新部署**

## 1. 起點

`/seed` 建立專案級規則檔;順帶確認 `docs/report20260721-1.md` 為未追蹤檔的處置;
最後把 `docs/todolist20260703-1.md`「規格回寫(維持 SSOT)」掛著的四項補完。

## 2. 執行內容

### 2.1 建立專案 `CLAUDE.md`(commit `b303d27`)

套 `~/.claude/templates/CLAUDE.dev.md`,上半部通用規範未改,只填專案區(78 行):

- **技術棧**:TS 5.5 ESM 純運算核心 + vitest + `@zxing/library`;demo 為原生 HTML 直接 `import ../dist/index.js`;規格 SSOT 指向主規劃書。
- **與通則不同**:① 改 `demo/*.html` 無自動化測試可覆蓋 → DoD 改為 build 後在 8765 測試台實測 ② 帳密/tier 為兩檔同步的權威表,解鎖判斷一律走 `flags.ts` 的 `isEnabled`。
- **已知的坑**:`dist/` 不進版控但 demo 依賴、部署要 rsync 到 `~/m-code-site`、`--bind ::` 否則 502、`.sh`/`.plist` 須 LF、白平衡是建議燈不可改回硬閘門。

### 2.2 確認 report20260721-1(commit `466068a`)

逐項核對後確認**該報告描述的工作全部有效**:`diff -rq` 比對 repo 與 docroot 的 `demo/`、`dist/`、`src/` 完全一致,本機與對外皆 200,`origin/main` 無落後;7/24 兩個 commit 也已上線(線上 `mobile.html` 含 `quickRow`)。唯一缺口是這份報告從未進版控(7/24 的 commit 只加了當日報告),已補。

### 2.3 規格回寫四項(commit `3a52c2b`)

| todolist 項次 | 落點 |
|---|---|
| 掃描清單 + PDF/Excel 報告輸出 | 主規劃書新增 **B6**:入口、表頭四欄位、清單項結構、PDF 首頁/次頁欄序、Excel 欄序與檔名、法律護欄逐字文案(並記下手機 vs 桌面欄數差:PDF 桌面多「工法/楞型」、Excel 桌面多「診斷」) |
| 四項互動細節 | 新增 **B7**:判定後可複製文字框(依 `full_parameters` 分流)、清單縮圖燈箱、自動開相機 + 500ms 即時量測迴圈(不解碼、幾何項每幀還原防殘留鎖死)、桌面展示/工程雙模式 |
| D4 「26.0mm 基準」 | **D4** 增段:26.0 mm = NT$10 硬幣直徑,為 `pxPerMm` 錨定長度;註明 demo 為示意文案、相機開啟後 `scaleRef` 誠實標未偵測 |
| C8 同步機制 | **C8** 補符號別涵蓋範圍(`LOW_CONTRAST`/`NO_DECODE` 跨符號通則),並訂「規格 JSON 為 SSOT、`rules.ts` 逐字轉寫、須同 commit 改」 |

連帶:B5 對應表 +3 列、C12 增「Demo 已實作、待原生落地」一組、全文目錄補 B6/B7、todolist 四項勾除並註明落點、修一處中國用語(A5 優化 → 最佳化)。

**同步機制落成程式**:新增 `tests/rules-spec-sync.test.ts` —— 讀主規劃書、切出 §C8 的 ```json 區塊、與 `SEED_DIAGNOSIS_RULES` 逐欄比對,任一邊單獨改動即測試失敗。

## 3. 驗證(DoD)

- `npm test` —— **13 檔 162 通過**(較先前 12 檔 159 增 3 測試)。
- `tsc --noEmit` 乾淨。
- **守門測試有效性實測**:臨時把 `rules.ts` 的 `BWR_GAIN.severity` 改 3→4,同步測試如預期失敗;`git checkout` 還原後 `git status` 確認無殘留。
- `git push origin main` 經 Eric 明確同意後執行:`695511a..3a52c2b`。

## 4. 未動與待辦

- **不需部署**:本次只改文件與測試,`src/`、`demo/`、`dist/` 未動,線上站與 repo 仍一致。
- `docs/todolist20260703-1.md` 的「產品/展示」3 項與「技術」3 項仍未開始(手機趨勢 teaser、桌面 tier 徽章、受楞痕影響標註;picket/px-per-module 即時化、參考卡真實偵測、手機量測四格)。
