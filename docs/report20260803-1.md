# 任務報告 — 階段 ④ 接線(梯形矯正接進檢驗流程)

> 2026-08-03 · M-Code · 對應 `docs/spec20260731-1.md` 階段 ④
> 起點 HEAD `0049109`(工作區乾淨,14 檔 336 測試全綠)

---

## 1. 這一輪做了什麼

三個裁示題卡住階段 ④ 已兩輪(見 `handoff.md`)。本輪先取得裁示,再依裁示接線。

**Eric 的裁示(2026-08-03):**

| 題目 | 裁示 |
|---|---|
| `focalPx` 從哪來 | **先試 `track.getSettings().focalLength`,拿不到退回臂長差代理值並在結果頁標註為代理** |
| iOS Safari tier-3 解析度 | **選 (b):先實機確認走 L2 還是 L3**,不改程式 |
| 本輪範圍 | **接線階段 ④** |

DEC 門檻是否重標定沒有問 —— 它的判準(實拍前後對照)不存在時預設就是「不得調整」,
規格 §6.1 已寫死,沒有裁示的餘地。

---

## 2. 施工中發現的問題(本輪最重要的產出)

**QR 的三個 finder 中心補出的第四角,推不出傾角 —— 而且恆推出 0.00°。**

規格 §3.3 的四角來源表原文寫「QR:3 點,第四角以 `tr + bl − tl` 推算 · 可行性:直接可做」。
接線時實測發現這條路走不通,原因是幾何上的:

`tr + bl − tl` 造出來的四邊形**依定義是平行四邊形**,而平行四邊形映到矩形的單應矩陣是
**仿射**的 —— 兩組對邊平行即消失點在無窮遠、消失線亦在無窮遠。`tiltFromHomography`
正是由消失線反推傾角,故**不論真實傾角多少一律算出 0.00°**。

針孔合成實測(400×400 方形、f=900px、距離 833;繞 X 軸與繞 Y 軸結果完全相同):

| 真實傾角 | 補點與真實第四角的距離 | 真四角推得 | **補點四角推得** |
|---|---|---|---|
| 0° | 0.0 px | 0.00° | 0.00° |
| 2° | 7.2 px | 2.00° | **0.00°** |
| 5° | 18.0 px | 5.00° | **0.00°** |
| 25° | 88.2 px | 25.00° | **0.00°** |

**0° 是 `gate.ts`「≤5° 否則 FAIL」最寬鬆的放行值。** 這與規格 §1.3 的即時迴圈 `tilt = 0`、
§1.5 的 `pxm = 9`、`tiltFromHomography` 初版的「焦距不可得回 0」是**同一個病灶** ——
用一個能通過型別檢查的假數字讓檢查看起來有在跑,而且**比現行的臂長差代理值更糟**
(代理值至少會隨傾斜變大)。

如果沒量這一下,接線之後 QR 的透視閘門會恆綠,而且從程式碼上看不出任何異常
(H 是合法矩陣、角度是有限數、退回路徑一條都不會觸發)。

**處理方式:** `quadFromZxingPoints` 改回傳 `{ corners, derived }`,`rectifyPlan` 對
`derived === true` 一律停用單應傾角與矯正影像,傾角退回臂長差代理值。護欄測試同時斷言
「真四角推得回真實傾角」作為對照組,證明失準來自補點而非管線。

**未解的部分:** 讓 QR 拿到真透視的唯一路徑是規格原文就提過的「有 alignment pattern 時
改用實測點」,但 ZXing 的 `QRCodeReader` 預設不外傳該點。已列為 §6.5 決策 ③ 待裁示。

---

## 3. 改了什麼

### 3.1 `demo/imgproc.js` — 新增五支純函式(接線判斷全部放這裡)

| 函式 | 職責 |
|---|---|
| `focalPxFromSettings(settings, imageWidthPx)` | 由 `getSettings()` 取像素焦距;換算尺度 + 合理帶把關,不可得回 `null` |
| `FOCAL_PX_LIMITS` | 凍結常數 `{ minRatio: 0.3, maxRatio: 5 }`(相對分析影像寬) |
| `quadFromZxingPoints(points)` | 依符號別補四角,回 `{ corners, derived }` |
| `rectifyQuad(gray, w, h, points, maxPixels)` | 四角 → 正射 ROI 的完整管線,退回時帶**指名原因** |
| `rectifyPlan(rect)` | 決定「量測吃哪張影像、傾角走哪條路徑」 |
| `resolveTiltDeg(H, focalPx, cx, cy, proxyDeg)` | 實算 → 代理 → 不可得,回 `{ deg, source }` |
| `TILT_SOURCE_LABEL` | 三個來源標籤,結果頁與 PDF 一律取自此表 |

**為什麼判斷要全部放進純函式:** 專案 `CLAUDE.md` 已載明 `demo/*.html` 無自動化測試可
覆蓋。「矯正採不採用」「傾角走哪條路」「退回原因是什麼」寫在 HTML 裡就永遠驗不了,
而這三件事正是本階段的誠實性核心。`mobile.html` 最後只剩座標換算與 DOM。

**`focalPxFromSettings` 的合理帶不是保守,是必要。** W3C Image Capture 的 `focalLength`
各家實作單位不一致,部分回**毫米**(如 4.5)。4.5 是有限正數,`tiltFromHomography`
不會拒絕它,會算出一個看起來像角度的數字 —— 又是同一個病灶。手機主鏡頭
f ≈ 0.6–0.87 W(視角 60–80°),望遠可到 2–3 W,故 `[0.3 W, 5 W]` 涵蓋所有實機鏡頭,
而毫米值在任何影像寬下都遠低於 0.3 W。另外 `settings.width` 可得時會先把焦距等比換算到
分析影像尺度 —— 第一層 ImageCapture 的照片寬遠大於串流寬,不換算會低估約 3 倍。

### 3.2 `demo/mobile.html` — 接線

- `roiGrayOf` 多回傳 `{ x0, y0, sx, sy }`,建立座標橋:
  **降取樣版 →(×kx,×ky)→ 全解析度 →(−ROI 原點,×sx,×sy)→ ROI 灰階**。
  三段都要走完,少一段 H 與影像就不在同一座標系。
- 新增 `rectifyRoi(zx, kx, ky, rg)`:把 ZXing 定位點換算進 ROI 灰階座標系後交給
  `rectifyQuad`。
- `inspectReal` 依 `rectifyPlan` 決定量測吃 `rect.gray` 還是 `rg.gray`;焦距與主點同樣
  換算到 ROI 灰階尺度後餵 `resolveTiltDeg`。
- 傾角來源為 `"none"` 時**不覆寫** `state.gate.tilt`。硬塞 `null` 會讓
  `classifyPerspective` 判 FAIL 而鎖死**所有 1D**(它們拿不到四角也拿不到臂長差)。
  這一點誠實地暴露了規格 §1.3 的病灶仍在,但修它屬 §6.3 的即時化待辦,不是本階段。
- 結果頁 `resultText` 新增一行「幾何量測:…;透視傾角 …(來源)」;
  掃描紀錄新增 `rectify` / `tiltSource` 兩欄,隨 PDF 明細輸出。
- `presentResult` 加第五個參數 `geom`,**模擬檢驗明確傳 `null`** —— 沿用上一張的摘要
  等於在沒有像素的情況下編造幾何量測結果。

### 3.3 `tests/imgproc.test.ts` — 新增 30 個測試(79 → 109)

涵蓋:焦距換算與合理帶邊界、毫米值必須被擋、四角補點與 `derived` 旗標、
**補點四角恆推 0°(附真四角對照組)**、`rectifyQuad` 的六條退回路徑各自的指名原因、
退回時 `gray` 必為 `undefined`、面積預算與 `MAX_WARP_PIXELS` 同源、
`rectifyPlan` 三條規則、`resolveTiltDeg` 的三種來源與「絕不代填 0」。

---

## 4. 驗收

### 4.1 實跑數字(非引用文件)

```
npm test        → Test Files 14 passed / Tests 366 passed   (改動前 336)
npm run typecheck → 通過
npm run build     → 通過
node --check(抽出 mobile.html 的 module script)→ 通過
```

`docs/spec20260731-1.md` §5.1 的基線表已當場加列 366。

### 4.2 本機實跑(1D 路徑,走完整條快門)

`demo/*.html` 沒有自動化測試可覆蓋,故用一份臨時探針 `demo/_probe.html`(**驗後即刪**)
把整條路徑真的跑一次:以 canvas 合成一張**真的 ITF 條碼**,經 `captureStream()` 當假相機
餵給 `getUserMedia`,不需要實體相機也不需要相機權限。

探針只動兩處,兩處都標明是探針:
① 替換 `navigator.mediaDevices.getUserMedia`;
② 繞過 `inspect()` 的 ARMED 檢查(合成全白圖必然眩光與楞痕不過,那是合成圖的性質,
不是閘門有問題)。

實測結果:

| 項目 | 實測 |
|---|---|
| 取幀路徑 | `L2/applyConstraints` · 請求 1272×280 · 串流 1272×280 · 分析 1272×280 |
| 解碼 | ✓ ITF-14 · `14712345678907`(ZXing 真的解開了合成圖) |
| 等級 | A · 通過(門檻 ≥ C,margin 2.50) |
| 結果頁新行 | 「幾何量測:四角不足(1D 掃描線僅兩端點,四角偵測屬階段 ⑤);透視傾角 —(未量測)」 |
| console | 整輪(載入 + 快門 + 檢驗)**零訊息**,無錯誤無警告 |

1D 的標註完全符合預期:拿不到四角、也拿不到臂長差,就誠實說「未量測」,不編造角度。

### 4.3 沒驗到的(誠實列出)

- **2D 路徑沒有實跑。** DataMatrix(四點實測 → 單應矩陣實算)與 QR(三點補點 → 代理值)
  只有 vitest 綠燈。兩者與 1D 共用同一段座標換算(`rectifyRoi`),那一段已被 1D 實跑
  走過,故殘餘風險集中在分支本身而非座標鏈 —— 但仍然是沒驗。
- **8765 測試台實機驗收沒做。** 依專案 DoD,`demo/*.html` 的改動要 build → rsync 到
  `~/m-code-site` → 實際操作。本輪只做到本機探針。
  **故階段 ④ 的狀態是「程式面完成 / 實機未驗」,不得宣稱完工。**
- **焦距實際拿不拿得到,不知道。** 合理帶只保證「單位錯的值會被擋掉並退回代理」,
  不保證真的拿得到可用焦距。假相機的 track 沒有 `focalLength`,所以本輪連一次
  `source === "homography"` 的實跑都沒有。

---

## 5. 文件回寫

- `docs/spec20260731-1.md` — §2 的 G3/G4 加註「僅部分成立」與成立條件;§3.3 新增補點段
  (含實測表);§3.3 焦距段記錄裁示與三支純函式;§4 階段表 ④ 改為「程式面完成 / 實機未驗」,
  並更正 `index.html` 不在範圍內;§5.1 加列 366;新增 §5.4b 階段 ④ 驗收;
  §6.5 決策 ① 記錄裁示、新增決策 ②(已裁示)與決策 ③(待裁示)。
- `docs/todolist20260703-1.md` — `focalPx` 那項打勾並記錄裁示;iOS 那項改為「只剩實機確認」;
  新增三項(2D 未實跑、8765 實機驗收、QR alignment pattern 待裁示)。

**一項規格更正:** §4 階段表原寫階段 ④ 會動到 `demo/index.html`。查證後不成立 ——
`index.html` 全檔沒有 `canvas` / `FileReader` / `Image()` / `getUserMedia`,也沒有
import `imgproc.js`,是純模擬台,`透視傾斜°` 是使用者拉的滑桿值。像素路徑只有
`mobile.html` 一條,沒有「接線」這件事可做。

---

## 6. 給下一輪的提醒

1. **階段 ⑤ 動工前先看 §6.5 決策 ③。** QR 的 alignment pattern 與 1D 的 bearer bar 都是
   「自己寫偵測拿實測角點」,合併做可能比分兩次划算。
2. **`rectifyPlan` 是唯一該加規則的地方。** 之後任何「這種情況要不要用矯正 / 要不要用單應
   傾角」的判斷都往那支加,不要回頭寫進 `mobile.html` —— 一寫進去就再也驗不了。
3. **假相機探針很好用,但別留在 repo 裡。** 需要時用本報告 §4.2 的做法重建即可:
   ITF 的編碼表只有 10 個 pattern,合成一張 ZXing 解得開的條碼約 20 行。
