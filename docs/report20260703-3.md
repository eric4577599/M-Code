# 任務報告 — PM 設計規劃審查 → 優化 → PM/Tester 複核(結案)

- **日期**:2026-07-03
- **任務**:PM 檢查設計規劃與優化後交 PM/Tester,迭代直到完成
- **結果**:PM 判定 **可交付** — 五項 P0 全數結案、Tester 六大項全 PASS、PM 追加兩小項也已修;測試 152/152、部署站同步

## 流程

1. **PM 初審**(general-purpose 代理,PM 視角):對照主規格與兩版 demo,審免費版旅程、免費/付費切分、D2 業務話術支撐力、規劃缺口 → 提出 5 項 P0、多項 P1。
2. **優化實作**:五項 P0 全修(見下表,commit `2e1f0bd`)。
3. **Tester 驗證**(tester 代理):vitest 152/152、tsc 乾淨、demo 語法、五項 P0 逐項 grep 佐證、部署站同步、模擬路徑回歸 — 六大項全 PASS。
4. **PM 複查**:親自核對程式碼後五項全 [結案],追加兩條小項並判「可交付」。
5. **收尾**:兩小項即修(情境按鈕不得演回 scaleRef、快門鎖死文案精確化),重新驗證部署。

## P0 修正明細(commit `2e1f0bd`)

| # | PM 發現 | 修正 |
|---|---|---|
| 1 | 免費版結果頁露出完整分級參數,與 upsell 自相矛盾 | 光度參數(SC/MOD/Rmin)照常顯示;幾何參數改「🔒 代號」鎖定樣式;判定後內容拆「基本參數(免費版·光度)」/「完整參數報告:付費版解鎖」 |
| 2 | 「比例尺卡已偵測」寫死,實機必穿幫 | 預設文案加「(示意)」;相機開啟後 scaleRef=false、文案改「未偵測 — 尺寸量測停用(不影響拍攝與分級)」(C4.2 語意) |
| 3 | 手機清單+PDF/Excel 牴觸「免費不留存」 | 清單頁 tierNote 橫幅明標付費版 Demo 試用;規格回寫列 P1 |
| 4 | 趨勢層級 C10(付費)vs B4(進階版)不一致 | 規格 B4 與桌面 demo 角標統一「付費版 · 需後台」 |
| 5 | ERP 殺手鐧(A5/C9)零展示 | 桌面結果頁新增「回寫 ERP(模擬)· 付費版」:GET 工單帶門檻 → POST + Idempotency-Key → 201 inspectionId → SYNCED,數值取自當筆檢驗 |

## PM 追加兩小項(本次已修)

- 相機已開時點示範情境,scaleRef 不得被演回 true(以實況為準,scaleNote 文字不失真)。
- 拍攝頁鎖死文案改「有 FAIL 燈時快門鎖死(比例尺卡除外—未偵測僅停用量測)」,五綠一紅可拍的現場提問有答案。

## P1 Backlog

→ 詳見 `docs/todolist20260703-1.md`(PM 同意列後補,不阻擋交付)。

## 驗證

- vitest **152/152** 通過、`tsc --noEmit` 乾淨、demo 行內 script `node --check` 通過
- 部署站 `~/m-code-site` 已同步(curl 驗證含新版標記)
