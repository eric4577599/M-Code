# 任務報告 — 規格層問題修復(report20260703-1 建議事項)

- **日期**:2026-07-03
- **任務**:依 `report20260703-1.md`「已發現但未修改」三項建議,修規格文件並同步程式,完成後 commit 與 push(經明確授權)
- **結果**:三項全數修復,規格與程式一致;測試 137 → **139 全過**,`tsc` 乾淨,`dist/` 重建

## 修復內容

### 1. GTIN 樣本 15 位數 → 合法 GTIN-14
規格 C9.2/C9.3 實例的 `104712345678904` 是 15 位數(GTIN-14 應 14 位)。統一改為 **`14712345678907`**(指示碼 1 + 471234567890 + 檢查碼 7,檢查碼經 GS1 演算法驗證)。

同步修改:
- 規格 md C9.2 `expectedGtin`、C9.3 `decodedData`
- demo 兩版 `SAMPLE_DATA.ITF14`;`GS1_128` 樣本一併改為 `(01)14712345678907`(原 `(01)04712345678901` 檢查碼也是錯的,現與 ITF-14 同一 GTIN,語意一致)
- demo HUD 與 SVG 快照的人讀字串改為 `1 47 12345 67890 7`
- `tests/erp.test.ts`(2 處)、`tests/pipeline.integration.test.ts`

### 2. 規格 C8 種子規則補齊 CODE128 / DATAMATRIX
- `NO_DECODE` appliesTo 補 `CODE128`(受支援符號解不出來卻不觸發 severity 3「不可讀」)
- `LOW_CONTRAST` appliesTo 補 `CODE128`、`DATAMATRIX`(低對比對任何符號都成立)
- 規格 md C8 JSON 與 `src/data/rules.ts` 同步修改

### 3. picket 補 FAIL 帶(>25°)
規格 C4.2 picket 原本 FAIL 欄留白,條碼橫躺 90° 也只 WARN 可拍,牴觸 D2「必須 picket fence」。
- 規格 md C4.2:picket FAIL 欄改為「>25°(D2:必須 picket fence,橫躺不得放行)」
- `src/engines/gate.ts` `classifyPicket`:≤10° OK、10–25° WARN、**>25° FAIL**(快門鎖死)
- 測試補 25.1° FAIL 與 90° 橫躺 FAIL 兩案例

## 驗證

- `npx tsc --noEmit` 乾淨;`npx vitest run` **139/139 通過**
- `npm run build` dist 重建;demo 行內 script `node --check` 通過
- 全 repo grep 確認無殘留舊 GTIN(`104712345678904` / `04012...` 系列樣本)
