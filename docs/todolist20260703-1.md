# 待辦清單 — P1 Backlog(PM 審查後補項,2026-07-03)

> 來源:PM 設計規劃審查(見 `report20260703-3.md`)。均不阻擋 demo 交付。

## 產品/展示

- [ ] 手機版加「趨勢 teaser」:鎖定樣式縮圖 +「付費版解鎖」,讓 D2 業務故事在手機上收尾(現需切桌面版)
- [ ] 桌面版加 tier 徽章:付費功能(量測/診斷/CSV/ERP/趨勢)標「付費版」,恢復免費/付費分層敘事
- [ ] 結果頁「受楞痕影響」標註:WARN 拍攝時,數值旁標示(規格 B2 的後半段)

## 技術

- [ ] picket(印向)與 px/module 燈號拍攝前即時化(現為拍後由 ZXing points 回填 / 預設值)
- [ ] 參考卡/硬幣真實偵測(兩版 demo 目前皆為示意);AE/AF/AWB lock 偵測
- [ ] 手機版量測四格(付費賣點)展示畫面
- [x] `src/domain/types.ts` 的 `CaptureQualityReport.gsdMmPerPx` 放寬為 `number | null`(來源:`spec20260731-1.md` 階段 ②)→ **2026-08-01 完成**,`pxPerModule` 一併放寬(同一個 NaN 承載法,不該只改一半)。掃過的消費端:`src/export/csv.ts`(§C9.4 欄位清單本來就不含這兩欄)、`src/erp/client.ts`(不觸及 `capture`)、`demo/*.html`(只往 `evaluateGate` 傳入,不讀報告的這兩欄)—— **無呼叫端需要改**,故無行為變更,只是把不可得從型別上暴露出來。守門測試已從「NaN 不得外洩」擴充為 null / NaN 兩種承載法都測(`tests/csv.test.ts`),並新增序列化往返測試(`tests/gate.test.ts`)證明 null 往返前後一致而 NaN 不然
- [x] **階段 ④ 接線前必須先確定 `focalPx` 來源** → **2026-08-03 Eric 裁示:先試 `track.getSettings().focalLength`,拿不到退回 `quadGeometry` 的臂長差代理值並在結果頁標註為代理。** 已落地為三支純函式(`focalPxFromSettings` / `resolveTiltDeg` / `TILT_SOURCE_LABEL`,共 30 個新測試)。`focalPxFromSettings` 另加了「合理帶」把關(`FOCAL_PX_LIMITS = {0.3, 5}` × 分析影像寬)—— W3C Image Capture 的 `focalLength` 各家單位不一致,部分回**毫米**(如 4.5),那是有限正數、`tiltFromHomography` 不會拒絕,會算出一個看起來像角度的假數字。**尚未驗證的是機型覆蓋率與單位**,實機才問得出來。見 `spec20260731-1.md` §6.5 決策 ②
- [ ] **iOS Safari tier-3 快門解析度** — **2026-08-03 Eric 裁示選 (b):先實機確認走 L2 還是 L3**,不改程式。待辦剩「拿一台 iOS 機在 8765 拍一次,回報 `camStatus` 的層級與 §5.2 的三個數字」。若實測走 L2 則問題自動消失。見 `spec20260731-1.md` §6.5 決策 ①
- [ ] **DataMatrix 路徑尚未實跑** — 1D 與 QR 兩條已在本機用假相機實跑過(見 `spec20260731-1.md` §5.4b / §5.4c),DataMatrix(四點實測 → 單應矩陣實算)只有 vitest 綠燈。三者共用同一段座標換算,故殘餘風險在分支本身
- [ ] **階段 ①②③④ 的 8765 實機驗收(一次做完)** — 依專案 DoD:`npm run build` → `rsync -a --delete --exclude node_modules --exclude .git ./ ~/m-code-site/` → 測試台實際操作。要記的數字見 `spec20260731-1.md` §5.2(Android 與 iOS 各一次:請求值 / `getSettings()` 實拿值 / 分析影像實際寬 / 實走 L1L2L3),順帶就把 §6.5 決策 ① 的前置一起做掉
- [x] **QR 要不要取 alignment pattern** → **2026-08-03 Eric 裁示 (c),QR 半邊當場做掉。** 查證後發現這不只是「拿不到真透視」而已 —— ZXing 對 v≥2 的 QR **本來就會回四個點**,第四個是 alignment 中心而非右下角,而上一輪的 `quadFromZxingPoints` 會把它當實測角點,導致**拍正的 QR 也算出 53.74° 傾角、每張 v≥2 QR 都 FAIL**(`quadConfidence` 擋不住)。已改為由模組座標還原符號真四角(`qrQuadFromPoints`,dimension 照抄 ZXing 的 `computeDimension`),並實跑驗證(本專案第一次走到「單應矩陣實算」)。見 `spec20260731-1.md` §3.3 alignment pattern 段與 §5.4c
- [ ] **階段 ⑤ 剩餘工作:1D 四角偵測** — ITF-14 靠 bearer bar、GS1-128 只能靠條的上下端點群做直線擬合。**四條邊必須各自獨立擬合**:若上下邊取成平行,得到的又是平行四邊形,傾角一樣恆 0°(見 §3.3 補點段的教訓)。擬合信心不足時誠實退回不矯正並標原因。規格 §5.4 的「1D 邊界擬合失敗」退回測試也要在此補齊

## 規格回寫(維持 SSOT)

**2026-07-29 的四項已全數回寫主規劃書 `corrugated_barcode_qc_app_master_v1.md`;2026-07-31 另起一項,見本段末。**

- [x] 掃描累積清單、PDF/Excel 報告輸出(表頭欄位、法律護欄文字)寫入規格 Part B / C12 → 新增 **B6**,C12 增列「Demo 已實作、待原生落地」
- [x] 判定後可複製文字框、清單縮圖燈箱、自動開相機+即時量測迴圈、桌面「展示/工程」雙模式補入規格 → 新增 **B7**(四項),B5 對應表增列三列
- [x] D4 補「26.0mm 基準」定義(NT$10 硬幣直徑),與 demo 文案對齊 → **D4** 增「基準 26.0 mm 的定義」段(錨定 `pxPerMm`、示意文案與 scaleRef 誠實化說明)
- [x] C8 診斷規則:確認 LOW_CONTRAST/NO_DECODE 補 CODE128/DATAMATRIX 後的規格文字與 rules.ts 長期同步機制 → **C8** 增符號別涵蓋範圍與同步紀律;機制為 `tests/rules-spec-sync.test.ts`(解析規格 JSON 與 `rules.ts` 逐欄比對,不一致即失敗)
- [x] C4.2 解析度閘門新增「GSD 不可得」分支(來源:`spec20260731-1.md` 階段 ②)→ 2026-07-31 回寫 **C4.2**:改為兩支判定表(GSD 可得 / 不可得)並列出兩個 `threshold` 字串、載明門檻不放寬與「不歸因」原則;C4.3 偽碼補 `gsd==null` 走向註解;C3.1 `CaptureQualityReport.gsdMmPerPx` 加註「不可得目前以 NaN 承載」
- [x] **C4.2 / C3.1 的規格↔程式同步無自動化守門** → **2026-08-01 完成**,新增 `tests/gate-spec-sync.test.ts`(36 個測試)。涵蓋:C4.2 六項檢查表的 key 與順序、每項 OK/WARN/FAIL 三欄的門檻數字(解析規格寫法還原成邊界值再實跑 `classifyChecks`)、附加的透視門檻、解析度兩支判定表與兩個 `threshold` 字串、「不可得」守衛表逐列、`GateCheck.value` 的哨兵值 `-1`、C3.1 `CaptureQualityReport` 的欄位名/順序/型別逐字比對。**刻意比對行為而非程式碼字面**(門檻寫在 `if` 裡,比字面等於再抄一次規格)。已用 9 個變異(3 個程式端 + 6 個規格端)驗證守門本身會紅,見 `report20260801-1.md` §3。
  - 施工當下就抓到一筆真實漂移:C4.2 表格的 `whiteBalance` 仍寫「>5% → FAIL」,與程式的建議燈語意及同節下方的守衛表矛盾,已更正規格(不動程式)
  - 仍未涵蓋:C4.2 六項檢查表的「量測方法」欄(自然語言,無從機械比對)、C4.1 狀態機、C4.3 偽碼
- [x] `CaptureQualityReport.gsdMmPerPx` 由 `number` 放寬為 `number | null` **之後**,同步回寫主規劃書 C3.1 的宣告與該處註解(移除「以 NaN 承載」的過渡說明)→ **2026-08-01 完成**,與型別放寬同一批改動。C4.2 那段「報告頂層仍沿用 NaN 約定、兩者尚未統一」也一併改寫為「依該欄型別放不放得進 null 而定」。**此欄從此有自動化守門**(`tests/gate-spec-sync.test.ts` 逐字比對 C3.1 宣告);另補記:回寫時人工發現規格的 C3.1 `CaptureQualityReport` 漏列 `measurementEnabled`,已補上(**是人先看到的,不是測試抓的** —— 補完才寫測試;事後把該行從規格刪掉重跑,確認守門的「欄位名與順序完全一致」那條的確會紅)
