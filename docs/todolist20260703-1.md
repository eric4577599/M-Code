# 待辦清單 — P1 Backlog(PM 審查後補項,2026-07-03)

> 來源:PM 設計規劃審查(見 `report20260703-3.md`)。均不阻擋 demo 交付。

## 產品/展示

- [ ] 手機版加「趨勢 teaser」:鎖定樣式縮圖 +「付費版解鎖」,讓 D2 業務故事在手機上收尾(現需切桌面版)
- [ ] 桌面版加 tier 徽章:付費功能(量測/診斷/CSV/ERP/趨勢)標「付費版」,恢復免費/付費分層敘事
- [ ] 結果頁「受楞痕影響」標註:WARN 拍攝時,數值旁標示(規格 B2 的後半段)

## 技術

- [ ] picket(印向)與 px/module 燈號拍攝前即時化(現為拍後由 ZXing points 回填 / 預設值)
- [ ] 參考卡/硬幣真實偵測(兩版 demo 目前皆為示意);AE/AF/AWB lock 偵測
- [ ] 手機版量測四格(付費賣點)展示畫面
- [ ] `src/domain/types.ts` 的 `CaptureQualityReport.gsdMmPerPx` 放寬為 `number | null`(來源:`spec20260731-1.md` 階段 ②)。現況是 `gate.ts` 以 `NaN` 承載「GSD 不可得」,而 **NaN 會傳染**:`JSON.stringify` 變 `null`、`toFixed()` 印出 `"NaN"`、任何算術一路污染下去。改成 `null` 後呼叫端才被型別逼著處理不可得的情形。目前靠 `tests/csv.test.ts` 的守門測試鎖住「CSV 不消費 `capture.gsdMmPerPx`」來擋 NaN 外流,治標;改型別時要一併掃過 ERP payload 與報告輸出
- [ ] **階段 ④ 接線前必須先確定 `focalPx` 來源** — `tiltFromHomography(H, focalPx, cx, cy)` 沒有焦距就推不出絕對傾角(H 只給得到消失線)。三個候選皆**未驗證**:`track.getSettings().focalLength`(機型覆蓋率與單位不明)/ 由已知尺寸基準物反推(依賴尚未實作的「參考卡真實偵測」,與真實 GSD 同一前置)/ 機型對照表(維護成本高)。**焦距定不下來,`spec20260731-1.md` §2 的 G4「`perspectiveTiltDeg` 改由單應矩陣推導」就不成立**,只能退回 `quadGeometry` 的臂長差代理值。函式端已做到誠實(不可得回 `null`,不再填 0 放行 —— 0 是 ≤5° 閘門最寬鬆的值,等同 §1.3 的病灶),**但退回路徑要由呼叫端寫,那是階段 ④ 的事**:接線時必須決定「拿不到焦距要顯示什麼、結果如何標註該值為代理」,不可讓 `null` 一路流進 `gate.ts`。候選比較見 `spec20260731-1.md` §3.3「焦距怎麼來」段
- [ ] **待 Eric 裁示** — iOS Safari tier-3 的快門解析度(選項 (a) 重開串流 / (b) 先實機確認走哪一層 / (c) 接受限制並在文案載明);決策內容與代價見 `spec20260731-1.md` §6.5 決策 ①,前置條件是拿一台 iOS 機實測走 L2 還是 L3

## 規格回寫(維持 SSOT)

**2026-07-29 的四項已全數回寫主規劃書 `corrugated_barcode_qc_app_master_v1.md`;2026-07-31 另起一項,見本段末。**

- [x] 掃描累積清單、PDF/Excel 報告輸出(表頭欄位、法律護欄文字)寫入規格 Part B / C12 → 新增 **B6**,C12 增列「Demo 已實作、待原生落地」
- [x] 判定後可複製文字框、清單縮圖燈箱、自動開相機+即時量測迴圈、桌面「展示/工程」雙模式補入規格 → 新增 **B7**(四項),B5 對應表增列三列
- [x] D4 補「26.0mm 基準」定義(NT$10 硬幣直徑),與 demo 文案對齊 → **D4** 增「基準 26.0 mm 的定義」段(錨定 `pxPerMm`、示意文案與 scaleRef 誠實化說明)
- [x] C8 診斷規則:確認 LOW_CONTRAST/NO_DECODE 補 CODE128/DATAMATRIX 後的規格文字與 rules.ts 長期同步機制 → **C8** 增符號別涵蓋範圍與同步紀律;機制為 `tests/rules-spec-sync.test.ts`(解析規格 JSON 與 `rules.ts` 逐欄比對,不一致即失敗)
- [x] C4.2 解析度閘門新增「GSD 不可得」分支(來源:`spec20260731-1.md` 階段 ②)→ 2026-07-31 回寫 **C4.2**:改為兩支判定表(GSD 可得 / 不可得)並列出兩個 `threshold` 字串、載明門檻不放寬與「不歸因」原則;C4.3 偽碼補 `gsd==null` 走向註解;C3.1 `CaptureQualityReport.gsdMmPerPx` 加註「不可得目前以 NaN 承載」
- [ ] **C4.2 / C3.1 的規格↔程式同步無自動化守門** — `tests/rules-spec-sync.test.ts` 只解析主規劃書 C8 的種子規則 JSON 與 `src/data/rules.ts` 逐欄比對,**不涵蓋 C4.2 的解析度閘門判定表,也不涵蓋 C3.1 的 `CaptureQualityReport` 型別宣告**。目前改 `gate.ts` 的判定分支或 `types.ts` 的欄位型別,全靠人工記得回寫規劃書,沒有任何測試會紅。同一類問題已實際發生過兩次(`CLAUDE.md` 測試基線過期、`spec20260731-1.md` §5.1 權威表自己過期),**人工紀律不是機制**。待評估:比照 C8 的做法,把 C4.2 的兩支判定表(GSD 可得 / 不可得)與門檻字串寫成可解析格式,讓測試逐欄比對;C3.1 則至少守住「欄位型別是否含 `null`」這一項
- [ ] `CaptureQualityReport.gsdMmPerPx` 由 `number` 放寬為 `number | null` **之後**,同步回寫主規劃書 C3.1 的宣告與該處註解(移除「以 NaN 承載」的過渡說明)。與「技術」段的型別放寬項綁在同一個 commit,不可只改程式不改規劃書 —— 此欄目前無自動化守門(`tests/rules-spec-sync.test.ts` 只比對 C8 的種子規則 JSON,不涵蓋 C3.1 / C4.2)
