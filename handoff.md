# Handoff — M-Code(瓦楞箱條碼 / QR 品質檢驗核心庫)

> 最後更新:2026-09-14T00:15+0800

## Current Task

**兩條線並行,而且互不相卡。**

**① 實機驗收仍是唯一的原始 blocker**(自 2026-08-07 起沒變)——
線上章 `77e6f7a20152`,§3.13–§3.17 那一輪相機/取幀重構已全部上線,**但一張都沒實拍過**。
見 Next Step 第 0 項。

**② 2026-08-22 跑完一輪全功能對抗性稽核,13 條 finding,報告 `report20260822-1.md`。**
七個切片(domain+gate / decode+measurement / grade+diagnosis+acceptance /
erp+csv+data / imgproc / mobile 狀態機 / PWA+部署鏈)全部打完。
**`src/` 零變更、門檻一行未動、尚未修任何一條。**
基線:16 檔 521 測試通過、typecheck / build 乾淨、線上 hash 一致、`__STAMP__` 殘留 0 ——
**十三條全部是在「測試全綠、部署正常」的前提下仍然成立的問題。**

修法全部不動門檻(符合 §6.1 凍結),所以**不必等實機就能動手**;
唯一的例外是 A-10,它會改變閘門實際嚴格度,修完前後的紀錄不可互比。

**08-24 已處理掉半條**:A-13 的驗法進了專案 `CLAUDE.md` 的「已知的坑」
(`grep -c __STAMP__` 必須為 0,且說明為何不可比 hash)。

**09-11 修掉五條**:
- **src/ 側四條**(A-07 / A-09 / A-04 / A-11):走 `pm-rd-tester` workflow 1 輪 PASS,
  測試 521→554(+33),三道閘門主 session 親自重跑 + 18 項獨立重演(不用 workflow 的測試)全過。
  報告 `report20260911-1.md`、規格 `spec20260911-1.md`。
- **demo 層第一條**(A-12 登出不清資料):兩檔同步,瀏覽器實際操作驗證 + 對照組 + 回歸。
  報告 `report20260911-2.md`。

**09-14 修掉 demo 層第二條**(A-02/03):加預期 GTIN 輸入欄,比對三態。
測試 554→584(+30),含突變驗證過的守門測試。報告 `report20260914-1.md`、規格 `spec20260914-1.md`。

**剩下六條半,全部在 demo 層**(A-10 / A-01 / A-05 / A-06 / A-08 + A-13 的另外半條)——
`typecheck` 只掃 `src/`、demo 無自動化測試,**workflow 的 Tester 層驗不到**,
故 src/ 那輪刻意不納入。這批的 DoD 是瀏覽器實際操作驗證,**且驗之前必須先清 SW 與 caches**
(見 B 線第 1 項的兩個 ⚠)。

> git 與上線狀態一律現查,本檔不記快照:`git log origin/main..HEAD --oneline` / `git status --short`。

## Done

> 只留結論。過程與證據在各自的報告裡,不在本檔。

| 日期 | 主題 | 結論 | 報告 |
|---|---|---|---|
| 07-31 | 階段 ①②③ | 解析度偵測 + 三層降級取幀、GSD 誠實化、梯形矯正核心純函式。**三階段皆實機未驗** | `report20260731-1.md` |
| 08-01 | C3.1 型別放寬 | 加 C4.2/C3.1 守門測試 | `report20260801-1.md` |
| 08-03 | 階段 ④ 接線 | 含決策 ③(c) 的 QR 半邊 | `report20260803-1.md` |
| 08-04 | 階段 ⑤ 1D 半邊 | `quadFrom1DEdges` 四邊獨立擬合,合成情境傾角誤差 ≤0.037° | `report20260804-1.md` |
| 08-05 | 對抗性稽核 + 六項裁示 | `src/` 零變更、門檻未動。測試 415→431。實測證實「光度崩塌是取樣範圍不同,不是矯正弄糊影像」;推翻規格 §1.5 表格第二列 | `report20260805-1.md` |
| 08-05 | 1D 元素計量重新設計 | **兩個失效都發生在像素完美的合成圖上**(與拍攝條件無關):量測模型套錯符號別、模組寬被量化成整數像素。新增 `roi1DMetrology`,測試 431→457。同一張 ITF-14 由 DEC F(0.00)→ A(3.99) | `report20260805-2.md` |
| 08-06 | 快門延遲取幀誠實性(§3.7) | 判定改成量在**被分析的那張影像**上。測試 457→471。推翻自己第一版:varLap 比值在靜止圖上擺動 1%–137%,不可當 stale 判準 | `report20260806-1.md` |
| 08-06 | PWA 離線外殼 | cache-first + 內容雜湊蓋章 + 使用者確認才更新。同日引入三個回歸(見 Risk) | `report20260806-1.md` |
| 08-07 | §3.13–§3.17 | `initCamera` 防重入 → 保底幀像素快照 → 挑幀留痕 → **移除 L2、預覽拉到 1920** → 讀數上結果頁。L2 移除的依據是實機四戰四敗 | `report20260807-1.md` |
| **08-22** | **全功能對抗性稽核** | **13 條 finding,`src/` 零變更。共同形狀:11 條在呼叫端 —— `typecheck` 只掃 `src/`、demo 層無自動化測試** | **`report20260822-1.md`** |

### 08-22 稽核的十三條(完整內容見報告)

| # | 嚴重度 | 一句話 |
|---|---|---|
| A-10 | 🔴 | **閘門量錯對象**:`gate.ts` 定義是 ROI,`measureQuality` 量全幀。失焦條碼配銳利印刷背景,全幀 30247 → 對焦 OK、ROI 58 → 應 FAIL |
| ✅ A-07 | 🔴 | `getWorkOrder` 裸 cast → ERP 回髒 `requiredGrade` 時 `pass=false` 卻 `margin=+3.60`,**汙染預警與趨勢** |
| ✅ A-02/03 | 🟠 | `expectedDataMatch` 拿解碼結果跟自己比,恆 `true`。mobile 潛伏(未存)、`index.html` 今天就顯示「比對符合」 |
| A-01 | 🟠 | 「本張閘門 全部 OK」含從未量過的項;**2D 的 `pxm` 從來不寫入**,解析度恆綠 |
| ✅ A-12 | 🟠 | `doLogout` 不清 `scans` 與表頭三欄 → 共用手機跨使用者殘留,新使用者可整批匯出 |
| A-06 | 🟡 | 結果頁可同時顯示 F 級與全 A 參數;`explainGrade` 修了「是哪一項」沒修「顯示什麼數字」 |
| A-05 | 🟡 | 付費文案承諾六項,mobile 實作約一項半;「尺寸量測」在實機**結構性不可得** |
| A-08 | 🟡 | QR/DM 允收門檻寫死在 `mobile.html:487` 的 fallback,不受 §6.1 治理、無守門測試 |
| ✅ A-09 | 🟡 | token 端點回「200+格式錯」→ 佇列永遠 QUEUED(與註解宣稱的收斂目標相反) |
| A-13 | 🟡 | 「忘記蓋章」無偵測,且**未蓋章的 sw.js 與 repo hash 一致** → 驗證慣用法對這支檔案方向相反 |
| ✅ A-04 | 🔵 | `measurement.ts` 守衛用 `<=0`,NaN/Infinity 穿透,但註解宣稱擋住了 |
| ✅ A-11 | 🔵 | `SCANLINE_SPREAD_HINT` 零測試覆蓋(它是 A-06 唯一的緩解機制) |

## Next Step

**兩條線可並行。稽核修復不卡實機,實機不卡稽核修復。**

### A 線:實機(原始 blocker,優先)

**不卡實機的項目已全部做完。** 順序:

0. **🔴 唯一的 blocker:拿 iPhone 14 Pro 拍幾張,讀結果頁上的四個數字。**
   **步驟**:開 https://m-code.ericchh.work/demo/mobile.html → **按「有新版本可用」橫幅**
   (離線外殼刻意不自動生效,不按就還在舊版)→ 確認快門下方版本章是 **`77e6f7a2`**
   → 對 ITF-14 拍幾張。四個數字現在**直接印在結果頁**(§3.17),不必匯出 PDF。
   **三種結果各自的下一步:**
   | 看到什麼 | 意思 / 接著做 |
   |---|---|
   | **串流** 不是 1920×1080 | 裝置給不到,§3.16 的前提要重談(`ideal` 不是 `exact`) |
   | **px/module ≥ 8 且讀得出來** | §3.16 成立,ITF-14 的結構性問題解決 → 換打 B1 與 E1 |
   | **px/module 達標卻仍不可讀** | `DECODE_MAX_W`(解碼吃 ≤1024 縮圖)就是瓶頸 —— **那時才有真實依據動它**(§3.17 記載了為何現在不動) |
   另外順手記:**快門延遲**應從實機的 1402–1522ms 掉到接近零(探針上 44ms);
   以及連拍幾張看**發熱與耗電** —— 1080p 預覽的成本是 §3.16 唯一沒量過的取捨。

0a. **✅ 已結案(§3.16)——「高解析 vs 即時」的對照不必做了。**
   它原本要問的是「iOS 升壓有沒有效」。答案由實機四張快門直接給了:**四次都協商到
   3024×4032、四次都被淘汰**,分析永遠落在預覽幀 —— 即選項 ①(升壓對該機無效)。
   故 L2 整條移除,兩種模式在 iOS 上現已等價,沒有東西可對照。
   ⚠ **但 §6.1 的門檻凍結不因此解除** —— 它要的是「提高解析度前/後」的實拍對照,
   而那組數字要等 Next Step 第 0 項(1920 預覽下的實拍)才拿得到。
0b. **⚠️ 上線前要先讓 Eric 知道:等級會整體上移。** ITF-14 從結構性的 F 變成真實量測,
   現場通過率會明顯改變 —— 這是**修正不是放寬**,但別被當成標準變鬆。
1. **8765 雙平台實機驗收(一次把 ①②③④⑤ 做完)** — 三步缺一不可,見 Key Context。
   **本輪新增兩個必記欄位**(已進 PDF「取幀」行,拍幾張就有):`取幀時序` 的
   **快門延遲毫秒數** 與 **對焦相對預覽 %**。前者是 `SETTLE_FRAMES` / `SLOW_SHUTTER_MS`
   兩個推理值的唯一校準來源;後者是「比值能不能升格成判準」的唯一依據 ——
   **在它穩定之前不要拿比值當門檻**。⚠️ 探針量到的 1992ms **不是有效數字**
   (非前景分頁下 rAF/rVFC 不觸發,三段等待每次吃滿逾時)。
   **必記欄位已擴充**,權威清單在規格 §5.2 那張四列表:
   `state.gate.wb`(牛皮箱面、自然光)/ `JSON.stringify(getSettings())` 全文 /
   鏡頭朝向 / 1D 矯正時 `geo` 的取樣尺寸。加上原本的請求值 / 實拿值 / 分析寬 / L1-L2-L3。
   ⚠️ **D5④ 修正之前取得的請求值一律作廢**(舊值會印出從未送出的請求)。
1b. **架構重評估已完成,結論在** https://claude.ai/code/artifact/c0ab7e69-867f-4cee-b6d9-8c0c6bb01626
   —— 四件事依序:① 實機驗收(卡實機)② PWA(**已做完**)③ 流程改連續評估 ④ 後端紀錄簿。
   **③ 是下一個大的**:把「快門」換成連續評估,判定的幀就是被分析的幀,延遲問題不是被
   補償而是不存在。前提(低解析度不再直接換算成等級)已由 §3.5 的計量重新設計滿足,
   但**誤拒率必須先有實拍數字**,所以排在實機驗收之後。
   **④ 的關鍵設計點**:後端只收不算,**等級不可變且蓋版本章** —— 昨天的 ITF-14 修正把同一張
   圖從 F 變成 A,後端若會重算,歷史紀錄的等級會整批靜默改變。
2. **押後的兩件演算法改動(D5 明確裁示留到下一輪)** — 1D 橫躺的方位處理(讓量測跟著符號
   主軸走)、`edgeFitRoi` 沿定位線法向而非影像 y 軸外擴。**兩件都要動演算法,
   應該連同實拍誤拒率一起看**,不要在沒有實拍數字時先改。
3. **取樣密度規則** — 低於門檻關掉 `useRectified`。要加就加在 `rectifyPlan`,且**必須等**
   §5.2 的實機數字(規格 §6.1 門檻凍結)。
4. **階段 ⑥ torch** — 需 Android 實機。

### B 線:稽核修復(不必等實機,全部不動門檻)

依 `report20260822-1.md` 的建議順序:

1. ~~**A-12 登出清資料**~~ —— **✅ 09-11 完成**(`report20260911-2.md`)。
   兩檔同步,並多清了稽核未點名的同類洩漏:結果頁節點、`resultText`、`lastShotInfo`、
   `lastResultBtn` 停用(B 原本可按「📋 檢測結果」讀到 A 的實拍影像與可複製判定文字)。
   ⚠ **驗 demo 前必須先清 Service Worker 與 caches**,否則驗到的是舊碼 ——
   repo 的 `sw.js` 未蓋章,快取名恆定為 `mcode-shell-__STAMP__`。細節見報告。
   ⚠ **8765 測試台的 docroot 就是 `~/m-code-site`(線上同一份)**,照字面在 8765 驗
   等於先發佈再驗證;09-11 改用臨時埠 8799 直接服務 repo,驗過才上線。
2. ~~**A-02 / A-03 比對誠實化**~~ —— **✅ 09-14 完成**(`report20260914-1.md`)。
   Eric 選「把功能做成真的」:兩檔加 `f-expected-gtin` 輸入欄,比對三態
   (符合 / 不符 / 未比對)。守門測試 `tests/expected-gtin-sync.test.ts` 30 支,
   **已用兩次突變證明它抓得到回歸**。
   ⚠ `expectedMatch` 只進 `scans`,**尚未進 CSV / PDF / Excel** —— 接上時
   `null`(未比對)必須輸出空字串,不得輸出 `false`。
3. **A-01 + A-10 一起修** —— 兩條同源,都是「本張閘門」在說謊,且都在快門後路徑、資訊現成:
   `shotGate` 改吃 ROI(`:837` 已有 `pmRoi`)+ 改三態呈現(量過且 OK / 量過且非 OK / **未量測**)。
   ⚠ **A-10 會改變閘門實際嚴格度**(全幀→ROI 等於變嚴),依 §6.1 修前修後紀錄不可互比 ——
   **這是真實成本,要排進計畫而非當收尾**。
4. ~~**A-07 `isWorkOrder` 守衛**~~ —— **✅ 09-11 完成**(`report20260911-1.md`)。
5. ~~**收尾**:A-04 / A-09 / A-11~~ —— **✅ 09-11 一併完成**。A-04 實作端全檔確認後從三處補到五處;
   `csv.ts:formatNumber` 非有限值改回空字串(原為字面 `NaN`)。
   **下一輪候選**:`washboard()` 的 `periodMm` 仍原樣穿透 NaN(非計算分支,出口端已由 CSV 空字串接住)。
6. **需要決策不只是修**:A-05(文案對齊實作,或反之)、A-08(QR/DM 門檻要不要進 `policies.ts`)。
7. **A-13 剩一半** —— 驗法已寫進專案 `CLAUDE.md`(08-24),但那只是「有人記得去驗」。
   真正的解仍是 `report20260807-1.md` D1 建議的 `deploy.sh`,讓
   build → rsync → stamp 三步不可能拆開。**做了才算關掉這條。**


## Key Context

- **git** — 分支 `main`。**本檔不再記錄「推到哪個 commit」** ——
  那種快照寫一次過期一次(2026-08-05 一天之內就過期三次:每次 push 完、
  或收工前補寫 handoff 之後,上一行就變成假的,下一個 session 會照著假資訊判斷)。
  **要知道有沒有未推的東西,一律現查:**
  ```bash
  git log origin/main..HEAD --oneline   # 空的 = 全部已推
  git status --short                    # 空的 = 工作區乾淨
  ```
  push 依全域 §0 需 Eric 明確同意;`fetch` / `pull` / `commit` 不在此限。
- **上線狀態同理,現查不要信快照** —— 線上 docroot 是 `~/m-code-site`,不是 repo 本身。
  上線要跑 `npm run build` → `rsync -a --delete --exclude node_modules --exclude .git ./ ~/m-code-site/`
  → **`python3 deploy/stamp-sw.py`**(蓋章這一步以前三處清單全都漏記,權威版見專案 `CLAUDE.md`)。
  **驗法不是看 HTTP 200,是比 hash**:
  ```bash
  for f in demo/imgproc.js dist/index.js; do
    a=$(curl -s https://m-code.ericchh.work/$f | shasum | awk '{print $1}')
    b=$(shasum "$f" | awk '{print $1}'); [ "$a" = "$b" ] && echo "$f 一致" || echo "$f 不同"
  done
  ```
  **任何 `.html` 的 hash 都會對不上,那是正常的**(不只 `mobile.html` —— 2026-08-07 上線時
  `index.html` 也對不上,一度誤以為沒同步)。成因是 Cloudflare 在 `</body>` 前注入
  bot-detection script,差異僅此一段。要確認就 `diff` 一下,只有那一行就是同步好了;
  HTML 改驗**內容**(例如 `grep -c camInitializing`)而不是 hash。
- **測試基線同樣現查,不寫快照**(2026-08-07 改)—— 本檔原本寫「14 檔 471」,
  而 2026-08-06 一天就漲到 **16 檔 521**,快照當場變成假的。與 git / 上線狀態同一個道理:
  ```bash
  npx vitest run --reporter=dot | tail -3   # 檔數與測試數以此為準
  npm run typecheck && npm run build        # 兩者都要過
  ```
  規格 §5.1 那張表仍是「歷史沿革」的權威(每階段收工加一列),
  **但不要拿它當現況** —— 它已經自己過期三次,§5.1 補記載明了建議改法。
- **實機驗收要跑的三步(缺一不可)** —
  `npm run build` → `rsync -a --delete --exclude node_modules --exclude .git ./ ~/m-code-site/`
  → **`python3 deploy/stamp-sw.py`** → 8765 測試台實際操作。
  **只 git pull 不 rsync = 線上不會變;rsync 了不蓋章 = 快取名不換,新舊版共用快取。**
- **沒有實機時怎麼驗 `demo/*.html`** — 假相機探針。**做法與 ITF 編碼表已寫進
  `report20260805-1.md` §5**(上一輪只寫了做法沒留碼,這次補上),照著重建即可。
  兩個省時間的點:① 快門 FAIL 時是 `disabled`,`.click()` 沒反應,改呼叫
  `document.getElementById('shutter').onclick()` 走的仍是同一條 `inspect()`;
  ② 驗 PDF 時先把 `window.print` 換成 no-op,否則列印對話框會卡住整個工具。
  **`report20260806-1.md` §4 又補了三個坑**:③ 插入點要用 `lastIndexOf("</body>")`——
  `exportPdf` 的 HTML 字串裡也有一個,`String.replace` 只換第一個會把探針塞進模組的
  樣板字串裡;④ 畫布重繪要 `setInterval` 不能 `requestAnimationFrame` —— 自動化跑在
  非前景分頁,rAF 完全不觸發 → `captureStream` 一幀都不產生 → 相機看起來像開不起來;
  ⑤ 登入直接寫 `sessionStorage["mcode.session"]` 再 reload,欄位 id 是 `loginUser`/
  `loginPass`/`loginBtn`。**探針走 loopback-only 的 8799 服務 repo 本體 ——
  8765 服務的是 `~/m-code-site`,它經 cloudflared 對外,探針不可放進去。**
  **探針檔驗完就刪**(本輪已刪並確認對外 404)。
- **文件** — 規格 `docs/spec20260731-1.md`(**新增 §3.4b 降級矩陣 / §3.4c 可量測性 /
  §3.4d 光度基底 / §3.5 1D 元素計量重新設計 / §3.7 快門延遲取幀誠實性**);報告 `report20260731-1.md`、
  `report20260801-1.md`、`report20260803-1.md`、`report20260804-1.md`、
  `report20260805-1.md`(稽核六項裁示,含探針原始碼)、
  **`report20260805-2.md`(1D 元素計量重新設計)**、
  **`report20260806-1.md`(快門延遲下的取幀誠實性,規格 §3.7)**。
  稽核決策頁:https://claude.ai/code/artifact/4a8a0961-41e9-4239-9cad-12e8c3420bad
  計量設計文件:https://claude.ai/code/artifact/d3291e96-0293-426b-a4ce-e49ae570697f
- **守門測試的分工** — `rules-spec-sync.test.ts` 守 C8;`gate-spec-sync.test.ts` 守 C4.2 + C3.1;
  `imgproc.test.ts` 守 `QUAD_CONFIDENCE_LIMITS` / `LINE_FIT_LIMITS` / `ONE_D_SYMBOLOGIES`
  整表,**外加可量測性護欄(文案不得含 ISO/合規/不合格/印壞)、光度基底對照,
  以及新的「取樣密度掃描」守門(6→14 px/元素、0.1 步進,等級擺動須 < 0.25 級 ——
  這一類病是改動前的 431 個測試全數漏掉的)**。

- **這個專案的 L2 教訓在哪** —— M-Code scope 現有四條:`feedback-handoff-no-state-snapshots`
  (本地)+ 三條 symlink 到 `-Users-hezhaoxing/memory/` 的正典檔
  (`feedback-acceptance-and-verification` / `feedback-code-audit-discipline` /
  `feedback-pm-rd-tester-workflow`)。**改正典檔會同時影響其他專案**,不要當成本專案私有。
  其中 `feedback-code-audit-discipline` 的實證正是 2026-08-22 這輪稽核。

## Risk / Note

- **稽核的 28 條 CONFIRMED 不代表我在瀏覽器裡重現了失效。** 那是「另一個 agent 打開行號
  重讀確認程式碼確實如此」。全部來自靜態閱讀與 Node 重演,**一條都沒實拍過**。
- **三條 UNCERTAIN 拿一次實機讀數就能定案**:牛皮紙上白平衡是否恆亮 WARN、
  單通道飽和是否高估 SC、`focalLength` 兩平台可得性。**不要當成已確認的事實引用。**
- **本輪沒驗到的**(`report20260805-1.md` §4.3 完整列出):
  ① D3 的「光度基底 未矯正 ROI」分支沒在瀏覽器走到(探針合成圖條端擬合失敗,調兩輪停手;
  該分支的實質行為已由 vitest 同圖對照直接證實);② D5① 的中斷偵測沒有實機驗
  (`ended`/`mute` 在假相機上不會自然發生);③ 實拍一次都沒有。
- **1D 走矯正在低取樣密度下會讓 DEC 變差**(5px/module 時 avgMaxDev 0 → 0.5)。
  本輪**刻意不改行為**,8765 驗收要記這組數字。
- **C1 是這條產品線最貴的教訓,不要退化。** 任何「把對邊取成平行」的簡化都會讓傾角恆 0°,
  而 0° 是 ≤5° 閘門最寬鬆的放行值。護欄測試(平行四邊形對照組)不得刪。
- **`roi1DGeometry` 仍存在且仍被匯出,但量測路徑已不再走它** —— 它留著只為那組
  「記錄病灶、不得回頭」的對照測試。**新程式碼不要再用它量東西**,改用 `roi1DMetrology`。
- **Code 128 的完整參考解碼表(103 個樣式)未實作** —— 模組型走「結構錨定 + 捨入」,
  標稱序列是推得的不是解碼得來的。ITF-14 走真正的參考重建,無此問題。
- **`rectifyPlan` 與 `assessMeasurability` 是唯二該加規則的地方。**
  「要不要用矯正 / 要不要用單應傾角」往前者加;「這種情況該不該出等級」往後者加。
  **一寫進 `mobile.html` 就再也驗不了。**
- **QR 的傾角只限 v≥2**;v1 沒有 alignment pattern,仍走補點 → `derived` → 代理值。
- **即時迴圈的 `pxm` 仍硬寫 9、`tilt` 仍還原 0°** —— 快門前 resolution / perspective 燈恆綠,
  屬 §6.3 待辦。**D5③ 修的是「把快門後量到的記下來」,不是讓它參與判定。**
- **焦距在實機上拿不拿得到,仍然不知道。** 這仍是最該驗的一件事。
- **不得調 `policies.ts` 的任何允收門檻** —— 需先做「提高解析度前 / 後」的實拍對照(§6.1)。
- **白平衡是歷史誤判熱區** —— 本輪只動 UI 與紀錄,**引擎與兩道守門測試一行未改**,
  硬閘門復辟的護欄完整保留。
- **`SETTLE_FRAMES = 1` 與 `SLOW_SHUTTER_MS = 400` 是推理值不是量測值,而且
  `SETTLE_FRAMES` 的效益從未被驗證過。** 非前景分頁下 rVFC 不觸發,它一次都沒真的沉澱過。
  留著的底氣是「閘門已量在被分析的影像上」——真抓到爛幀會被告警,不會靜默出等級。
  **實機的 `取幀時序` 若顯示沉澱毫無作用就直接拿掉。**
- **⚠ 本機探針量不出任何時間效益,一毫秒都量不出。** 非前景分頁把 `setTimeout` 對齊到
  約 1 秒(實測 `setTimeout(500)` → **998ms**、`setTimeout(60)` → **471ms**),
  所有逾時被拉平 —— F4 下修與 F6 前後量到的 `快門延遲` 完全相同(1993/1989/1988ms)。
  **報告裡任何「快門延遲 ~1990ms」都不是延遲的真實量測值**,只證明欄位有接上。
  時間必須在實機上量,探針只能驗功能。
- **對焦門檻在合成圖上比 ZXing 還寬容** —— 糊到對焦不過(blur 5)時解碼在 blur 3 就先死了,
  所以「解碼成功 + 對焦 FAIL」這個組合在本輪合成圖上**做不出來**(改用眩光路徑替代驗證)。
  這是測試影像的性質(6px 模組、近二值對比),不是分支失效的證據 —— 但也還沒被驗過。
- **`pendingDownscale` 是新的跨函式狀態** —— `captureViaConstraints` 把降壓 promise 存進去、
  `inspect()` 的 finally 負責等完才清 `inspecting`、`stopCamera` 負責清空(不可跨串流殘留)。
  三處要一起看;少了 inspect 那一段,快門會在串流過渡中就重新開放。
- **離線外殼讓「線上沒更新」多了一種新形態** —— 現在有三份「app 由哪些檔案組成」的清單:
  `src/index.ts`(真來源)、HTML 的 `modulepreload`、`sw.js` 的 `SHELL`。
  漂開的症狀是**沒有症狀**(線上正常、只是離線能力沒了)。
  `tests/modulepreload-sync.test.ts` 與 `tests/pwa-shell-sync.test.ts` 守著,別刪。
- **外殼漏檔靜態檢查抓不到** —— 第一版漏了 `icons/*.png`(manifest 被解析時瀏覽器會去抓)。
  靜態檢查看不出「誰會被請求」,只有實跑逐一比對「啟動所需 vs 快取命中」才發現。
  **日後加任何會被瀏覽器自動請求的資源(字型、圖示、預載)都要重跑這個比對。**
- **PWA 沒有在手機上測過,也沒有真的斷網測過。** 驗的是等價條件(20 個資源全數命中、
  網路傳輸 0 KB)。iOS 的 SW 生命週期較積極回收,行為可能與桌面 Chrome 不同。
- **`explainGrade` 對 1D 不可以說「等級由 X 決定」** —— 1D 總分是逐條取最差再平均,
  而 `GradeResult.parameters` 只回報第 1 條。改文案時務必看 `src/engines/grade.ts` 的
  聚合註解,測試已釘住「規則說明必須明講不等於任何單一參數」。
- **「即時取幀」是使用者明選,不是預設。** 實拍誤拒率仍未知;敢給這個選項是因為 §3.5
  之後等級不再隨取樣密度擺盪(2.67 級 → 0.00–0.03 級),但那是合成圖的結論。
  **要改預設必須先有 0a 那組實拍對照。**
- **🔴 2026-08-06 一天內我引入三個回歸,模式相同** —— 現場壓力下改一個症狀,
  而新程式碼自己的失敗模式沒被同等強度地驗過。**改帶狀態的東西(相機、快取、旗標)時,
  「新增的失敗路徑會不會鎖死既有流程」必須當成必驗項**,不能只驗「原本症狀好了沒」。
  兩樣解藥已就位:§3.11 的致命錯誤出口(它上線當天就抓到我自己的兩個錯)、
  以及**注入式驗證**(驗 §3.12 時不靠假相機碰運氣,直接把 `play()` reject 與
  `track.ended` 注進去 —— 假相機不必然重現真實失敗)。
- **稽核 C 未修、待 Eric 裁示**:掃描清單只在記憶體,重整就全消失(實機已丟過資料)。
  選項:(a) 存 localStorage(我的建議)/ (b) 只在離開時警告 / (c) 等後端紀錄簿一起設計。
- **`camFault` 的設計原則**(§3.12 的教訓):故障旗標**由當下可觀測的狀態決定**,
  不可被某一刻的一次失敗鎖死。日後任何新增的「設 camFault」路徑都要問:
  這是暫時性還是永久性?有沒有對應的清除路徑?
- **SessionStart hook 的日期不可信** —— 文件命名前先跑 `date`(本輪已核對:2026-08-06 09:53)。

### 08-22 稽核新增的邊界(引用前必讀)

- **十三條一條都沒有實拍驗證。** 全部來自靜態閱讀 + Node 重演 + **合成輸入**。
  A-06 與 A-10 的**發生頻率**取決於真實影像特性,那組數字要等實機。
  機制確認 ≠ 現場一定會看到。
- **我在稽核過程中有四個假設被自己推翻,記著避免重犯**:
  ① 「條碼佔比小 → varLap 被稀釋 → 誤判 FAIL」**錯了** —— 銳利條碼佔 1.6% 畫面仍有 636 分。
  **危險的是反方向(背景紋理撐起讀數)**;② 「幾何寫入晚於 `gateOn`」錯了,全部早於;
  ③ 「`stamp-sw.py` 只雜湊清單不雜湊內容」**完全錯誤**,它連 `sw.js` 自己的原文都雜湊進去;
  ④ 「`clearList` 是死碼」錯了,grep 帶括號漏掉綁定行。
  **共同教訓:靜態閱讀足以產生方向相反的錯誤結論,CONFIRMED 一定要重演。**
- **A-01 是既有 Risk 那條「即時迴圈 pxm 仍硬寫 9」的延伸,但更糟**:
  該條原本的說法是「D5③ 修的是把**快門後量到的**記下來」——
  而 **2D 根本沒有「快門後量到的」**(`state.gate.pxm` 全檔唯一寫入點在 1D 分支內)。
- **A-06 是既有 Risk 那條「`explainGrade` 對 1D 不可以說『等級由 X 決定』」的延伸**:
  規則文案已守住,但 `byCode` 仍用第 1 條掃描線的 parameters 建,
  所以印出來的 letter / score 是第 1 條的 —— 可能與它宣稱的「拖低等級」完全相反。
- **A-12 與既有的「稽核 C 未修」(清單只在記憶體,重整全消失)是一體兩面** ——
  C 是「該留的沒留」,A-12 是「該清的沒清」。**兩者要一起設計**,
  否則修了 C(存 localStorage)會讓 A-12 從「重整就消失」惡化成「永久殘留」。
  這一點在決定 C 的選項 (a)/(b)/(c) 之前必須先想清楚。
- **A-05 與定位護欄有關**:付費文案承諾的「尺寸量測(X寬/BWR/Quiet Zone)」
  在實機上**結構性不可得**(`initCamera` 無條件把 `scaleRef` 壓成 false)。
  這不是「還沒做」,是承諾了該部署形態下交付不了的東西。
- **稽核未涵蓋**:`demo/manual.html`、`deploy/` 的 launchd / cloudflared 設定、
  規劃書與程式的逐條對照。
