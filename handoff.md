# Handoff — M-Code(瓦楞箱條碼 / QR 品質檢驗核心庫)

> 最後更新:2026-08-06T20:40+0800

## Current Task

`spec20260731-1.md` 影像層六階段。**階段 ①②③④⑤ 程式面完成但實機未驗;⑥ 未開始**。

2026-08-05 插入一輪**對抗性稽核 + 六項裁示的實作**(`report20260805-1.md`)。
稽核提出 33 條、CONFIRMED 28。Eric 六項全採建議((b))。**已全部實作完畢。**

2026-08-06 追加一輪 **快門延遲下的取幀誠實性**(`report20260806-1.md`,規格 §3.7)——
起因是 Eric 回報「拍照與取像有延遲,導致拍照有問題、無法判斷品質」。**已實作完畢。**

## Done

- **階段 ①②③(2026-07-31,`report20260731-1.md`)** — 解析度偵測與三層降級取幀、GSD 誠實化、
  梯形矯正核心純函式。**三階段皆實機未驗**。
- **C3.1 型別放寬 + C4.2/C3.1 守門測試(2026-08-01,`report20260801-1.md`)**。
- **階段 ④ 接線(2026-08-03,`report20260803-1.md`)** + **決策 ③(c)的 QR 半邊**。
- **階段 ⑤ 的 1D 半邊(2026-08-04,`report20260804-1.md`)** — `quadFrom1DEdges` 四條邊各自
  獨立擬合;合成情境傾角誤差最大 0.037°;三條分支(1D / QR v≥2 / DataMatrix)都用探針實跑過。
- **對抗性稽核 + 六項裁示實作(2026-08-05,`report20260805-1.md`,本輪)** ——
  **`src/` 零變更,`policies.ts` 門檻一行未動。** 測試 415 → **431**。
  - **D5①** 相機串流中斷偵測:`camFault` + `watchTrackHealth`(ended/mute/unmute)+
    `visibilitychange` + 取幀前 `cameraFaultNow()` 複驗。**是可用性守衛不是品質閘門。**
  - **D5②** 模擬模式全程標記:快門標籤、結果頁標記條、清單「示範」、PDF 標記區塊、
    XLS「資料狀態」欄,且**不計入通過率分母**。
  - **D5③** 快門後閘門讀數:`inspectReal` 補跑一次 `evaluateGate` → `lastShotInfo.shotGate`
    → PDF「本張閘門」。**不鎖快門、不改分級。**
  - **D5④** `recordShot` 請求欄:各層傳自己真正送出的那組,沒送出印「未請求」。
  - **D4** 量不到就不出等級:純函式 `assessMeasurability`(`imgproc.js`),三條路徑同一出口;
    1D 掃描線全滅時**拔掉了硬寫的 `{0.5,0.5,0.5}` 兜底**;橫躺給轉正指引。
  - **D3** 光度基底與幾何分離:1D `useRectified` 時幾何吃 `rect`、光度吃 `rg`;2D 不變。
  - **D1** 白平衡移出燈號面板(現在四盞燈),讀數改存進 shot → PDF。
    **`classifyWhiteBalance` 與兩份守門測試一行不動。**
  - **D2** 規格新增 §3.4b 能力→功能→降級矩陣;程式加 `samplingNote()` 中性取樣註記。
  - **D6** 規格 §5.2 加四列必記欄位表(零程式改動)。
- **本輪被實測證實 / 推翻的事**
  1. **稽核宣稱的光度崩塌是真的**:同圖對照下矯正影像的 `minEdgeContrast` **0.654 → 0.000**,
     而 `rLight`/`rDark` 兩側差距 < 0.25 —— **證明不是矯正把影像弄糊,是取樣範圍不同**。
     0° 拍攝同樣成立,與傾角無關。已成為 repo 內的守門測試。
  2. **規格 §1.5 表格第二列從未成立**(規格第三次自己過期)—— 原文「快門後依真實
     pxPerModule 判定,可能 WARN/FAIL」,實際上 `finally` 會先蓋回 0/0/9。已回寫。
  3. **一個因果誤解要改掉**:拍攝頁 perspective 燈恆綠 **跟焦距拿不拿得到無關**,
     是 `resetPreviewGeometry()` 每 500ms 無條件寫回 0。就算焦距拿得到、實測 30°,燈一樣綠。
  4. 實作到一半自己收緊了一條:原本 `simulated` 會 short-circuit 掉解碼判定,瀏覽器實跑
     發現「示範 + 不可讀」照樣吐等級 B —— 那正是要拔掉的心智模型。改成兩軸獨立。

- **1D 元素計量重新設計(2026-08-05 深夜,`report20260805-2.md`)** ——
  先證明「數不對」斷在哪:**兩個失效都發生在像素完美、零雜訊的合成圖上**(與拍攝條件無關)。
  **A 量測模型套錯符號別** —— ITF-14 是雙寬符號卻被當模組型量,GS1 建議的 2.5:1 離最近整數
  剛好 0.5 = 容差 ⇒ DEC 恆 0(F),6/12/24 px 全部一樣,**提高解析度永遠無效**。
  **B 模組寬被量化成整數像素** —— 5.5 px → F、14.2 px → A,同一張完美圖只是換個距離拍。
  取樣密度掃描(6→14 px,0.1 步進)現行擺動 **2.67 級**,而門檻只差 C 一級。
  新增 `roi1DMetrology`(合成孔徑 → 次像素邊界 → 元素數驗證 → 由解碼結果取回標稱序列 →
  依符號別分流的可解碼性),`src/` 零變更、門檻未動,測試 431 → **457**。
  **實跑翻轉:同一張 ITF-14 由 DEC F(0.00) → A(3.99)、等級 F 未通過 → C 通過。**
  實作中推翻自己文件的兩個判斷:① 孔徑不必等實拍(D3 已把光度與幾何拆開,孔徑只碰幾何);
  ② 模組型不能用無約束擬合(墨量增益下收斂到錯解 module 8.56 vs 真值 12),改用
  Code 128 的固定結構錨定 `w = 總跨距 ÷ 總模組數`。

- **快門延遲下的取幀誠實性(2026-08-06,`report20260806-1.md`,規格 §3.7)** ——
  **`src/` 零變更、門檻一行未動、本輪沒有引入任何新的判定門檻。** 測試 457 → **471**。
  診斷出兩個**與影像演算法無關**的時序缺陷:
  **① 判定的幀不是被分析的幀** —— 燈號量的是快門前 ≤500ms 的預覽,被分級的是快門後
  ≤800ms 的照片(最壞相隔約 1.3 秒);而昨天才加的「本張閘門」(D5③)光度四項讀的是
  `state.gate` 預覽殘值,**從未量過被分析的那張影像** —— 印「全部 OK」時講的是另一張。
  **② L2 抓的是 `applyConstraints` 換模式後的第一幀**(AE/AF 尚未收斂)。
  修法核心是**把判定改成量在被分析的那張影像上**(延遲多久都不影響結論):
  抽出 `measureQuality` 讓預覽與快門照走同一條 320px 取樣路徑、`shotGate` 改吃照片讀數、
  新增純函式 `assessShotFreshness`(「預覽過了、照片沒過」→ 具名告警)、
  實測並記錄快門延遲毫秒數、L2 升壓後多等 3 幀、快門標籤改「拍攝中 — 請維持不動」。
  **實跑證據**:快門後才加眩光 → `本張閘門 眩光 FAIL` + 告警,而預覽當下無眩光 ——
  舊程式碼會照抄那個 OK。**推翻自己第一版的判斷**:原打算用 varLap 比值當 stale 判準,
  實測同一張靜止合成圖上比值在 **1%–137%** 擺動,改成只作診斷數字、判定走既有絕對門檻。
  **同日 Eric 推翻了沉澱等待的折衷**(「3 幀、快門與間隔都過慢,反而會因手震導致問題」)——
  推翻得對:**等待本身就是手震窗口**,位移是連續必然的、AE/AF 未收斂只是一次性過渡,
  拿一定會發生的去換可能發生的是賠本。`SETTLE_FRAMES` 3 → **1 幀 / ≤60ms**;
  另追加 **F6:L2 的降壓等待移出關鍵路徑** —— 它發生在照片已取到之後,對拍到哪一幀
  毫無影響,原本卻 `await` 在關鍵路徑上,白白把結果頁往後推 300ms(只在升壓超過 1280
  時發生,即實機)。改由 `inspect()` 的 finally 在清 `inspecting` 前等,保護區間不變。

- **離線外殼 PWA(2026-08-06,`report20260806-1.md` §7)** —— 起因是「手機網頁打不開」。
  診斷:不是程式壞掉,是**往返次數** —— 20 個請求、僅 176KB,但 Cloudflare 邊緣在 SIN、
  origin 在台灣,單一 1KB 模組往返 660ms(origin 本機只要 1ms),且 `no-store` 讓每次開頁
  都完整重抓。兩段修正:`modulepreload` 把模組瀑布 4 層壓成 1 層(3760 → 3410ms),
  再加 Service Worker 離線外殼 —— **DOMContentLoaded 3760 → 67ms、網路傳輸 0 KB**。
  **更新刻意不自動生效**:`install` 內不 `skipWaiting`,新版只待命,由頁面跳橫幅、
  使用者按了才切換(這個專案踩過「線上沒更新」的坑)。快取名帶內容雜湊,
  `deploy/stamp-sw.py` 在 rsync 之後蓋章,不靠人記得改版本號。
  **`m-code-serve.py` 的 `no-store` 一行未動** —— 外殼接手後它不再是瓶頸,
  原本要問「要不要放寬快取」那題自動消失。測試 481 → **493**。

- **實機回饋的三輪修正(2026-08-06 傍晚,`report20260806-1.md` §8、規格 §3.8)** ——
  Eric 用 **iPhone 14 Pro** 在現場實測,回報三件事,全部已處理:
  1. **「再拍一次看不到 live 畫面」** —— `.view { display:none }` 把拍攝頁藏起來時,
     **iOS 會暫停那個 `<video>`,回來不會自動續播**;而 `visibilitychange` 不觸發
     (分頁仍可見,只有元素被藏)所以 `camFault` 也不亮 —— 一片黑且無說明。
     補三處:`show("capture")` 主動續播叫不醒才報故障、**暫停中不更新燈號**
     (原本拿暫停前那一幀刷新對焦/眩光 = 用舊畫面判定)、**暫停中不准按快門**
     (原本會取到凍結幀,產出看起來正常卻與現場無關的報告)。後兩項比黑畫面本身危險。
  2. **「報告沒有照片,只能憑感覺」** —— 結果頁加實拍影像 + **疊上實際量測範圍(ROI)框**
     (框是疊上去不是燒進去,ROI 存分數座標,原圖保持乾淨);新增純函式 `explainGrade`
     講「為什麼是這個等級」。**它最重要的工作是不要說謊**:2D 總分是所有參數的最小值
     可以指名,**但 1D 是逐條取最差再平均、回報的 parameters 只是第 1 條**,
     所以 1D 只能說「最常成為限制項的是 X(n/N 條)」。逐條資料靠對每條各跑一次
     `buildGradeResult` 取得,**不動 `src/`**。另印逐條離散度(>1.0 級提示重拍,
     措辭指向拍攝不穩**不指向印刷品質**,不參與評級)。PDF 明細同步。
  3. **「仍會延遲,按快門與實際不同」** —— iOS Safari 無 `ImageCapture`,L1 直接跳過,
     **每張都走 L2 升壓**;而 iOS 的 `applyConstraints` 常常不改解析度,沒生效時
     那段等待純虧。加「**高解析 / 即時**」切換(快門正上方,**預設維持高解析**,
     不靜默改變量測條件)。即時模式直接畫下當下幀,不呼叫 takePhoto、不下約束、
     不等協商。探針對照:快門延遲 **2002ms → 3ms**,而串流/分析/px per module/等級
     四項完全相同(假相機無升壓能力,正是「升壓沒生效」那一種)。

- **2026-08-06 傍晚實機場次(iPhone 14 Pro,`report20260806-1.md` §8、規格 §3.8–§3.12)** ——
  Eric 現場連續回報,逐項修:離線外殼(載入 3760→67ms)、結果頁實拍照片 + ROI 框 + 等級成因、
  取幀候選挑選(升壓幫倒忙就退回預覽幀)、1D 取樣範圍改由影像量出、致命錯誤出口 + 版本章。
  **但當天由我引入三個新缺陷**:① 無數據下把 `SETTLE_FRAMES` 3→1(實拍證明不夠);
  ② 離線外殼 fetch 用全域 `caches.match()` 造成「新 HTML 配舊 JS」;
  ③ `resumePreview()` 把 `play()` reject 當故障 → 第一張拍完就永久鎖死快門。
  **共同模式:在現場壓力下改一個症狀,而新程式碼自己的失敗模式沒被同等強度地驗過。**

- **設計規劃流程檢視 + 全程式碼逐檔審查(2026-08-07,`report20260807-1.md`)** ——
  流程 7 條、程式 20 條,涵蓋 `src/` 全 16 檔、`imgproc.js`、`mobile.html`、`sw.js`、`index.html`。
  **本輪只修了 1 條**(C1 = 規格 §3.13,見 Next Step 0aa),其餘全部只記錄未動。
  流程診斷一句話:**紀律品質極高,機制品質極低** —— 找到的問題幾乎都是
  「文件寫了正確的自我要求,但沒被寫成測試或腳本」;而五組 `*-sync.test.ts`
  守住的東西一次都沒漂過。**未修但值得先看的三條**(細節在報告):
  **B1** `barBandExtent` 條帶擴張沒有上限,楞紋背景會吃掉整張影像(觸發條件正是本專案場景);
  **S1** `WASHBOARD_AMP_RATIO_FLOOR` 的防漂註解沒接上,`gate.ts` 沒 import 它,已漂成三個數字;
  **E1** `src/` 約 38% 在實拍路徑上從未被呼叫,C8 診斷被 `PARAM_CAUSE` 平行重寫,
  而 `BWR_GAIN`(柔印墨量過多,severity 3)所需的 `barWidthGainMm` 根本沒被算出來 ——
  **這條要 Eric 裁示**:接上實拍路徑,還是承認已被取代並把 `rules.ts` 標為未接線。

## Next Step

**不卡實機的項目已全部做完。** 順序:

0aa. **🟠 blocker 已找到根因並修好,但**仍未在實機驗過**(2026-08-07,`report20260807-1.md`)。**
   **版本疑點已排除** —— 線上 docroot 的 `demo/*.html`、`imgproc.js`、`dist/*.js` 與 repo HEAD
   逐檔一致,`sw.js` 的章與內容相符。Eric 回報時跑的就是含 §3.12 的版本,**那個修正確實沒解掉**。
   **真正的根因是 `initCamera` 沒有防重入(規格 §3.13)** —— `if (camVideo) return` 讀的是
   `await` **之前**的值,而 `camVideo` 要到 `getUserMedia` resolve(最長 8 秒)之後才賦值。
   注入式實測(還原版 = 當時線上程式碼):**單純載入頁面就有 2 次並行 `getUserMedia`、
   洩漏 1 條仍 live 的串流**(`enterApp` 與 `pageshow` 同時觸發,`pageshow` 正常載入也會 fire);
   `pagehide` 後連點三下再多 3 次、洩漏 3 條。洩漏的 track 仍握著鏡頭 →
   下一次 `getUserMedia`「既不 resolve 也不 reject」= 相機失聯。
   §3.12 修的是 `camFault` 被鎖死,本條是串流被洩漏、`camFault` 全程 `null` —— **兩個獨立成因**。
   修法:`camInitializing` 旗標 + `camGeneration` 序號(作廢時把串流 stop 掉)。
   修正後三項情境全過(見規格 §3.13 表)。
   **⚠ 仍待實機:** 假相機沒有 iOS「同時只給一條串流」的排他性,實機表現可能是
   「第二條卡到逾時」而非「兩條都 resolve」。根因與修法相同,但要 iPhone 14 Pro
   連續兩張以上才算驗過。**本輪未上線**(未 rsync、未蓋章)。
   仍未做的:**相機生命週期的可見紀錄**(C3)—— `pagehide`/`pageshow`/`stopCamera` 被誰呼叫、
   `play()` 何時 reject 都還是沒留痕,而 `stopCamera` 會清掉 `lastShotInfo`。
   若實機證明 §3.13 仍沒解掉,**先補紀錄再修**,不要再猜。

0a. **⏳ 接著等的:iPhone 14 Pro 上「高解析 vs 即時」的同標的對照。**
   同一個條碼、手不移動、兩種模式各拍一張,比 PDF 那兩行「取幀」的三欄:
   `串流` / `分析` / `快門延遲`。**兩種結論:**
   ① 串流一樣大 → iOS 升壓對該機無效,那段等待純虧 → iOS 預設改即時,延遲歸零。
   ② 高解析明顯更大 → 升壓有效,是「解析度換延遲」的真實取捨,要看等級差多少。
   **這組數字就是規格 §6.1 卡住不准動門檻的那個「實拍對照」**,拿到才有資格往下走。
0. **⚠️ 上線前要先讓 Eric 知道:等級會整體上移。** ITF-14 從結構性的 F 變成真實量測,
   現場通過率會明顯改變 —— 這是**修正不是放寬**,但別被當成標準變鬆。
1. **8765 雙平台實機驗收(一次把 ①②③④⑤ 做完)** — 三步缺一不可,見 Key Context。
   **本輪新增兩個必記欄位**(已進 PDF「取幀」行,拍幾張就有):`取幀時序` 的
   **快門延遲毫秒數** 與 **對焦相對預覽 %**。前者是 `SETTLE_FRAMES` / `SLOW_SHUTTER_MS`
   兩個推理值的唯一校準來源;後者是「比值能不能升格成判準」的唯一依據 ——
   **在它穩定之前不要拿比值當門檻**。⚠️ 探針量到的 1992ms **不是有效數字**
   (非前景分頁下 rAF/rVFC 不觸發,三段等待每次吃滿逾時)。
   **必記欄位已擴充**,權威清單在規格 §5.2 那張四列表:
   `state.gate.wb`(牛皮箱面、自然光)/ `JSON.stringify(getSettings())` 全文 /
   鏡頭朝向 / 1D 矯正時 `geo` 的取樣尺寸。加上原本的請求值 / 實拿值 / 分析寬 / L1-L2-L3。
   ⚠️ **D5④ 修正之前取得的請求值一律作廢**(舊值會印出從未送出的請求)。
1b. **架構重評估已完成,結論在** https://claude.ai/code/artifact/c0ab7e69-867f-4cee-b6d9-8c0c6bb01626
   —— 四件事依序:① 實機驗收(卡實機)② PWA(**已做完**)③ 流程改連續評估 ④ 後端紀錄簿。
   **③ 是下一個大的**:把「快門」換成連續評估,判定的幀就是被分析的幀,延遲問題不是被
   補償而是不存在。前提(低解析度不再直接換算成等級)已由 §3.5 的計量重新設計滿足,
   但**誤拒率必須先有實拍數字**,所以排在實機驗收之後。
   **④ 的關鍵設計點**:後端只收不算,**等級不可變且蓋版本章** —— 昨天的 ITF-14 修正把同一張
   圖從 F 變成 A,後端若會重算,歷史紀錄的等級會整批靜默改變。
2. **押後的兩件演算法改動(D5 明確裁示留到下一輪)** — 1D 橫躺的方位處理(讓量測跟著符號
   主軸走)、`edgeFitRoi` 沿定位線法向而非影像 y 軸外擴。**兩件都要動演算法,
   應該連同實拍誤拒率一起看**,不要在沒有實拍數字時先改。
3. **取樣密度規則** — 低於門檻關掉 `useRectified`。要加就加在 `rectifyPlan`,且**必須等**
   §5.2 的實機數字(規格 §6.1 門檻凍結)。
4. **階段 ⑥ torch** — 需 Android 實機。

## Key Context

- **git** — 分支 `main`。**本檔不再記錄「推到哪個 commit」** ——
  那種快照寫一次過期一次(2026-08-05 一天之內就過期三次:每次 push 完、
  或收工前補寫 handoff 之後,上一行就變成假的,下一個 session 會照著假資訊判斷)。
  **要知道有沒有未推的東西,一律現查:**
  ```bash
  git log origin/main..HEAD --oneline   # 空的 = 全部已推
  git status --short                    # 空的 = 工作區乾淨
  ```
  push 依全域 §0 需 Eric 明確同意;`fetch` / `pull` / `commit` 不在此限。
- **上線狀態同理,現查不要信快照** —— 線上 docroot 是 `~/m-code-site`,不是 repo 本身。
  上線要跑 `npm run build` → `rsync -a --delete --exclude node_modules --exclude .git ./ ~/m-code-site/`。
  **驗法不是看 HTTP 200,是比 hash**:
  ```bash
  for f in demo/imgproc.js dist/index.js; do
    a=$(curl -s https://m-code.ericchh.work/$f | shasum | awk '{print $1}')
    b=$(shasum "$f" | awk '{print $1}'); [ "$a" = "$b" ] && echo "$f 一致" || echo "$f 不同"
  done
  ```
  `demo/mobile.html` 的 hash **會對不上,那是正常的** —— Cloudflare 在 `</body>` 前
  注入 bot-detection script,差異僅此一段,別誤判成沒同步。
- **測試基線同樣現查,不寫快照**(2026-08-07 改)—— 本檔原本寫「14 檔 471」,
  而 2026-08-06 一天就漲到 **16 檔 521**,快照當場變成假的。與 git / 上線狀態同一個道理:
  ```bash
  npx vitest run --reporter=dot | tail -3   # 檔數與測試數以此為準
  npm run typecheck && npm run build        # 兩者都要過
  ```
  規格 §5.1 那張表仍是「歷史沿革」的權威(每階段收工加一列),
  **但不要拿它當現況** —— 它已經自己過期三次,§5.1 補記載明了建議改法。
- **實機驗收要跑的三步(缺一不可)** —
  `npm run build` → `rsync -a --delete --exclude node_modules --exclude .git ./ ~/m-code-site/`
  → 8765 測試台實際操作。**只 git pull 不 rsync = 線上不會變**。
- **沒有實機時怎麼驗 `demo/*.html`** — 假相機探針。**做法與 ITF 編碼表已寫進
  `report20260805-1.md` §5**(上一輪只寫了做法沒留碼,這次補上),照著重建即可。
  兩個省時間的點:① 快門 FAIL 時是 `disabled`,`.click()` 沒反應,改呼叫
  `document.getElementById('shutter').onclick()` 走的仍是同一條 `inspect()`;
  ② 驗 PDF 時先把 `window.print` 換成 no-op,否則列印對話框會卡住整個工具。
  **`report20260806-1.md` §4 又補了三個坑**:③ 插入點要用 `lastIndexOf("</body>")`——
  `exportPdf` 的 HTML 字串裡也有一個,`String.replace` 只換第一個會把探針塞進模組的
  樣板字串裡;④ 畫布重繪要 `setInterval` 不能 `requestAnimationFrame` —— 自動化跑在
  非前景分頁,rAF 完全不觸發 → `captureStream` 一幀都不產生 → 相機看起來像開不起來;
  ⑤ 登入直接寫 `sessionStorage["mcode.session"]` 再 reload,欄位 id 是 `loginUser`/
  `loginPass`/`loginBtn`。**探針走 loopback-only 的 8799 服務 repo 本體 ——
  8765 服務的是 `~/m-code-site`,它經 cloudflared 對外,探針不可放進去。**
  **探針檔驗完就刪**(本輪已刪並確認對外 404)。
- **文件** — 規格 `docs/spec20260731-1.md`(**新增 §3.4b 降級矩陣 / §3.4c 可量測性 /
  §3.4d 光度基底 / §3.5 1D 元素計量重新設計 / §3.7 快門延遲取幀誠實性**);報告 `report20260731-1.md`、
  `report20260801-1.md`、`report20260803-1.md`、`report20260804-1.md`、
  `report20260805-1.md`(稽核六項裁示,含探針原始碼)、
  **`report20260805-2.md`(1D 元素計量重新設計)**、
  **`report20260806-1.md`(快門延遲下的取幀誠實性,規格 §3.7)**。
  稽核決策頁:https://claude.ai/code/artifact/4a8a0961-41e9-4239-9cad-12e8c3420bad
  計量設計文件:https://claude.ai/code/artifact/d3291e96-0293-426b-a4ce-e49ae570697f
- **守門測試的分工** — `rules-spec-sync.test.ts` 守 C8;`gate-spec-sync.test.ts` 守 C4.2 + C3.1;
  `imgproc.test.ts` 守 `QUAD_CONFIDENCE_LIMITS` / `LINE_FIT_LIMITS` / `ONE_D_SYMBOLOGIES`
  整表,**外加可量測性護欄(文案不得含 ISO/合規/不合格/印壞)、光度基底對照,
  以及新的「取樣密度掃描」守門(6→14 px/元素、0.1 步進,等級擺動須 < 0.25 級 ——
  這一類病是改動前的 431 個測試全數漏掉的)**。

## Risk / Note

- **稽核的 28 條 CONFIRMED 不代表我在瀏覽器裡重現了失效。** 那是「另一個 agent 打開行號
  重讀確認程式碼確實如此」。全部來自靜態閱讀與 Node 重演,**一條都沒實拍過**。
- **三條 UNCERTAIN 拿一次實機讀數就能定案**:牛皮紙上白平衡是否恆亮 WARN、
  單通道飽和是否高估 SC、`focalLength` 兩平台可得性。**不要當成已確認的事實引用。**
- **本輪沒驗到的**(`report20260805-1.md` §4.3 完整列出):
  ① D3 的「光度基底 未矯正 ROI」分支沒在瀏覽器走到(探針合成圖條端擬合失敗,調兩輪停手;
  該分支的實質行為已由 vitest 同圖對照直接證實);② D5① 的中斷偵測沒有實機驗
  (`ended`/`mute` 在假相機上不會自然發生);③ 實拍一次都沒有。
- **1D 走矯正在低取樣密度下會讓 DEC 變差**(5px/module 時 avgMaxDev 0 → 0.5)。
  本輪**刻意不改行為**,8765 驗收要記這組數字。
- **C1 是這條產品線最貴的教訓,不要退化。** 任何「把對邊取成平行」的簡化都會讓傾角恆 0°,
  而 0° 是 ≤5° 閘門最寬鬆的放行值。護欄測試(平行四邊形對照組)不得刪。
- **`roi1DGeometry` 仍存在且仍被匯出,但量測路徑已不再走它** —— 它留著只為那組
  「記錄病灶、不得回頭」的對照測試。**新程式碼不要再用它量東西**,改用 `roi1DMetrology`。
- **Code 128 的完整參考解碼表(103 個樣式)未實作** —— 模組型走「結構錨定 + 捨入」,
  標稱序列是推得的不是解碼得來的。ITF-14 走真正的參考重建,無此問題。
- **`rectifyPlan` 與 `assessMeasurability` 是唯二該加規則的地方。**
  「要不要用矯正 / 要不要用單應傾角」往前者加;「這種情況該不該出等級」往後者加。
  **一寫進 `mobile.html` 就再也驗不了。**
- **QR 的傾角只限 v≥2**;v1 沒有 alignment pattern,仍走補點 → `derived` → 代理值。
- **即時迴圈的 `pxm` 仍硬寫 9、`tilt` 仍還原 0°** —— 快門前 resolution / perspective 燈恆綠,
  屬 §6.3 待辦。**D5③ 修的是「把快門後量到的記下來」,不是讓它參與判定。**
- **焦距在實機上拿不拿得到,仍然不知道。** 這仍是最該驗的一件事。
- **不得調 `policies.ts` 的任何允收門檻** —— 需先做「提高解析度前 / 後」的實拍對照(§6.1)。
- **白平衡是歷史誤判熱區** —— 本輪只動 UI 與紀錄,**引擎與兩道守門測試一行未改**,
  硬閘門復辟的護欄完整保留。
- **`SETTLE_FRAMES = 1` 與 `SLOW_SHUTTER_MS = 400` 是推理值不是量測值,而且
  `SETTLE_FRAMES` 的效益從未被驗證過。** 非前景分頁下 rVFC 不觸發,它一次都沒真的沉澱過。
  留著的底氣是「閘門已量在被分析的影像上」——真抓到爛幀會被告警,不會靜默出等級。
  **實機的 `取幀時序` 若顯示沉澱毫無作用就直接拿掉。**
- **⚠ 本機探針量不出任何時間效益,一毫秒都量不出。** 非前景分頁把 `setTimeout` 對齊到
  約 1 秒(實測 `setTimeout(500)` → **998ms**、`setTimeout(60)` → **471ms**),
  所有逾時被拉平 —— F4 下修與 F6 前後量到的 `快門延遲` 完全相同(1993/1989/1988ms)。
  **報告裡任何「快門延遲 ~1990ms」都不是延遲的真實量測值**,只證明欄位有接上。
  時間必須在實機上量,探針只能驗功能。
- **對焦門檻在合成圖上比 ZXing 還寬容** —— 糊到對焦不過(blur 5)時解碼在 blur 3 就先死了,
  所以「解碼成功 + 對焦 FAIL」這個組合在本輪合成圖上**做不出來**(改用眩光路徑替代驗證)。
  這是測試影像的性質(6px 模組、近二值對比),不是分支失效的證據 —— 但也還沒被驗過。
- **`pendingDownscale` 是新的跨函式狀態** —— `captureViaConstraints` 把降壓 promise 存進去、
  `inspect()` 的 finally 負責等完才清 `inspecting`、`stopCamera` 負責清空(不可跨串流殘留)。
  三處要一起看;少了 inspect 那一段,快門會在串流過渡中就重新開放。
- **離線外殼讓「線上沒更新」多了一種新形態** —— 現在有三份「app 由哪些檔案組成」的清單:
  `src/index.ts`(真來源)、HTML 的 `modulepreload`、`sw.js` 的 `SHELL`。
  漂開的症狀是**沒有症狀**(線上正常、只是離線能力沒了)。
  `tests/modulepreload-sync.test.ts` 與 `tests/pwa-shell-sync.test.ts` 守著,別刪。
- **外殼漏檔靜態檢查抓不到** —— 第一版漏了 `icons/*.png`(manifest 被解析時瀏覽器會去抓)。
  靜態檢查看不出「誰會被請求」,只有實跑逐一比對「啟動所需 vs 快取命中」才發現。
  **日後加任何會被瀏覽器自動請求的資源(字型、圖示、預載)都要重跑這個比對。**
- **PWA 沒有在手機上測過,也沒有真的斷網測過。** 驗的是等價條件(20 個資源全數命中、
  網路傳輸 0 KB)。iOS 的 SW 生命週期較積極回收,行為可能與桌面 Chrome 不同。
- **`explainGrade` 對 1D 不可以說「等級由 X 決定」** —— 1D 總分是逐條取最差再平均,
  而 `GradeResult.parameters` 只回報第 1 條。改文案時務必看 `src/engines/grade.ts` 的
  聚合註解,測試已釘住「規則說明必須明講不等於任何單一參數」。
- **「即時取幀」是使用者明選,不是預設。** 實拍誤拒率仍未知;敢給這個選項是因為 §3.5
  之後等級不再隨取樣密度擺盪(2.67 級 → 0.00–0.03 級),但那是合成圖的結論。
  **要改預設必須先有 0a 那組實拍對照。**
- **🔴 2026-08-06 一天內我引入三個回歸,模式相同** —— 現場壓力下改一個症狀,
  而新程式碼自己的失敗模式沒被同等強度地驗過。**改帶狀態的東西(相機、快取、旗標)時,
  「新增的失敗路徑會不會鎖死既有流程」必須當成必驗項**,不能只驗「原本症狀好了沒」。
  兩樣解藥已就位:§3.11 的致命錯誤出口(它上線當天就抓到我自己的兩個錯)、
  以及**注入式驗證**(驗 §3.12 時不靠假相機碰運氣,直接把 `play()` reject 與
  `track.ended` 注進去 —— 假相機不必然重現真實失敗)。
- **稽核 C 未修、待 Eric 裁示**:掃描清單只在記憶體,重整就全消失(實機已丟過資料)。
  選項:(a) 存 localStorage(我的建議)/ (b) 只在離開時警告 / (c) 等後端紀錄簿一起設計。
- **`camFault` 的設計原則**(§3.12 的教訓):故障旗標**由當下可觀測的狀態決定**,
  不可被某一刻的一次失敗鎖死。日後任何新增的「設 camFault」路徑都要問:
  這是暫時性還是永久性?有沒有對應的清除路徑?
- **SessionStart hook 的日期不可信** —— 文件命名前先跑 `date`(本輪已核對:2026-08-06 09:53)。
